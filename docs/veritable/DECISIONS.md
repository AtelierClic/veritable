# Véritable — journal des décisions

Une entrée par décision, datée, jamais supprimée : une décision annulée reçoit une nouvelle entrée qui la remplace. Claude Code ajoute une entrée à chaque choix structurant pris en session (forme d'une donnée, modification de `src/core`, constante d'équilibrage majeure, écart par rapport à DESIGN.md). Format : date, décision, raison, ce que ça exclut.

## 2026-09-20 — Cadre initial (entretien 1)

- **Solo local, campagne persistante, une seule nation, bac à sable sans victoire.** Libère le déterminisme client/serveur ; exclut tout retour multijoueur sans réécriture.
- **Base OpenFront.io (AGPL-3.0), pas un mod.** Combat, économie et IA remplacés ; carte, tuiles, diplomatie, nucléaire, structures conservés.
- **Distribution Windows via Electron.** Tauri en repli.
- **Données réelles, ~195 nations, économie détaillée, politique interne complète, blocs dirigeables.**
- **Guerre libre mais sanctionnée ; nucléaire utilisable avec conséquences ; effondrement = gouvernement en exil.**
- **Événements scriptés et procéduraux ; plafond technologique spéculatif lointain.**

## 2026-09-21 — Décisions de conception (entretien 2)

- **Nom du projet : Véritable.**
- **Le joueur est l'État, pas le gouvernement.** Un changement de dirigeant, un coup ou une révolution ne terminent rien. Exclut le mode « perdre l'élection = fin ».
- **Temps : 1er janvier 2026 ; à ×1, 1 minute réelle = 1 mois de jeu ; vitesses pause / ×1 / ×2 / ×5.** Sauvegarde auto par mois de jeu, pas par jour.
- **Économie de biens en paliers : 12 biens au palier 1, ~40 au palier 2.** Un prix mondial par bien + flux bilatéraux. Exclut une économie à or unique.
- **Dette/PIB avec taux, austérité forcée ou défaut, réprimande des blocs (UE 3 % / 60 %).**
- **Sanctions = embargos par bien et par pays**, effets sur PIB, budget, opinion, tension, contournement, flux d'armes, présence militaire. Test de la sanction cosmétique au J3a.
- **Simulation politique asymétrique.** Politique complète pour le joueur ; régime + stabilité scalaire + traits du dirigeant + agenda pour l'IA. Exclut la symétrie, y compris plus tard.
- **9 archétypes de régime** : parlementaire, présidentiel, semi-présidentiel, autoritaire électoral, parti unique, monarchie absolue, junte, théocratie, État failli.
- **Élections orientables** par propagande, contrôle des médias, fraude, clientélisme — chacun avec son risque.
- **Chute de l'État** : exil tant que reconnaissance + soutien > seuil ; sous le seuil ou annexion reconnue → dissolution → baroud d'honneur sur un petit État de la même campagne.
- **Dirigeants = entités à traits, source Wikidata.** Noms parodiques par défaut, interrupteur « fictif » dans les données. Risque de droit à l'image connu et accepté.
- **Modèle de front : ensemble des tuiles frontalières, segments, divisions affectées sans position exacte, postures, résolution par rapport de force** (modèle Hearts of Iron).
- **J3 scindé** : J3a terre + réaction internationale ; J3b marine, air, logistique, effectifs.
- **Structures OpenFront conservées** : villes, postes de défense, ports, navires, silos, SAM.
- **Nucléaire : doctrine par nation dans les données ; main morte à l'annexion, probabilité visible ; tout tir = réaction internationale maximale.**
- **Blocs = entités complètes** (membres, adhésion, sortie, règle de décision par domaine, budget, compétences, leadership), en trois couches. Palier 1 : UE, OTAN, BRICS, ASEAN, UA, Mercosur, OPEP, CEDEAO, Ligue arabe, OCS, G7, G20.
- **Frontières de facto (Natural Earth), territoires occupés = tuiles du contrôleur étiquetées « contesté ».** Exclut les frontières de jure.
- **Micro-États = nations sans tuile, rattachées à une tuile hôte.**
- **Le monde démarre avec les conflits en cours**, listés dans le scénario et validés à la main.
- **Arbre technologique : tronc commun + branches de doctrine par bloc**, en paliers ; réserve du premier entretien levée.
- **Noyau opinion/stabilité dès le J2**, avant la guerre.
- **Gratuit ; contenu et interface en français, code en anglais, clés i18n dès le départ ; modding ouvert.**
- **Distribution itch.io ou GitHub Releases, binaire non signé** (avertissement SmartScreen accepté), pas de Steam. Budget hors Claude : 10 €.
- **Rupture avec l'amont OpenFront** : le fork ne fusionne plus jamais après le commit de départ.
- **RNG seedé obligatoire** dans toute la simulation.
- **Modèles Claude Code** : Fable 5.1 pour J0 et l'architecture ; Opus 5 pour les systèmes spécifiés ; Sonnet 5 pour le volume ; `opusplan` en session courante.

## 2026-09-21 — J0 : socle (session Claude Code)

### Décisions structurantes

- **Drapeau unique `GameConfig.veritable`.** Toute modification de `src/core` est derrière ce drapeau (`Config.isVeritable()`), pour que les tests OpenFront restent verts. Toute partie solo est une campagne Véritable (`veritableSoloConfig`, appliqué dans `joinLobby`) ; le multijoueur hérité reste tel quel, sans maintenance. Exclut un mode solo « OpenFront classique ».
- **« À l'identique » = identité du fichier de sauvegarde (option A).** snapshot → restauration → snapshot donne les mêmes octets. Le rejeu du journal de commandes (option B) est écarté : temps de chargement proportionnel à la durée de campagne.
- **La simulation vit dans le worker, à côté du `GameRunner`** (`VeritableSession`). Le client passe par `RemoteVeritableSim` (mêmes opérations que `VeritableSim`, asynchrones ; pas d'`advance` côté client : le worker avance la sim à chaque tick du cœur). Le runner headless du J2 réutilisera `VeritableSession`.
- **Frontière sim ↔ monde tuilé : interface `WorldPort`** (`tileCounts`, `capture`, `restore`), définie dans `sim/`, implémentée par `adapters/CoreBridge.ts` et par `sim/testing/MemoryWorld.ts`. `sim/` n'importe ni `src/core` ni le client.
- **Rattachement des tuiles.** La nation est l'entité ; la tuile pointe vers elle. Dans le cœur : `smallID` sur 12 bits (inchangé). Dans `CoreBridge` : table `smallID ↔ NationId`. Dans la sauvegarde : index dans `nations[]` + 1 (0 = sans propriétaire), bit 13 = retombées. Jamais de `smallID` dans une sauvegarde.
- **`NationState`** : `id`, `name` (`{kind:"key"}` | `{kind:"literal"}`), `regime` (nullable au J0), `territory` (`tiles` | `microstate`), `status` (`active` | `exiled` | `dissolved`), `tileCount` (cache, jamais un critère de vie), `isPlayer`. Au J0 seul `active ↔ exiled` est dérivé du territoire ; `dissolved` n'est jamais dérivé d'un nombre de tuiles (J7).
- **Restauration = recréer le jeu cœur depuis `world.coreStart` (GameStartInfo, JSON canonique à clés triées), puis reposer l'état par une `Execution`** (`RestoreExecution`, dans `tick()` et non `init()` : le cœur perd les exécutions ajoutées pendant une phase d'`init`). Elle tourne dans un tick pour que tuiles, joueurs et unités atteignent le client par le flux normal de mises à jour. La session est créée avant `GameRunner.init()` pour que la restauration passe avant les `NationExecution` (sinon les nations re-spawnent par-dessus la sauvegarde). Le tick de restauration n'est pas du temps de campagne.
- **Conteneur `.vsave`** : `"VRTB"` · `schemaVersion` u16 (lu avant tout décodage) · longueur + en-tête zbin · longueur + bloc de tuiles. zbin n'ayant ni octet de version ni étiquettes de champ, **le schéma d'en-tête de chaque version est figé** (`data/schemas/save.ts` → `SaveHeaderV1Schema`) et enregistré dans `HEADER_CODECS` ; toute évolution = nouveau schéma `VN` + migration `from: N-1` dans `save/migrations/`. Chaîne vide en v1 mais toujours exécutée par `decodeSave` ; testée avec une fausse v0.
- **Compression des tuiles : RLE + varints (LEB128), TypeScript pur, synchrone, déterministe.** Pas de `CompressionStream` (asynchrone, incertain sous jsdom). Mesures (territoire de Voronoï couvrant toutes les terres, pire cas réaliste) : `giantworldmap` 4108×1948 = 8,0 M tuiles, 195 nations → **188 208 octets** (brut 16,0 Mo, ×85 ; encodage 55 ms, décodage 8 ms) ; 107 nations → 157 294 octets ; `channelislands` 4744×2628 = 12,5 M tuiles (plus grande carte du dépôt), 44 nations → 60 306 octets. Partie réelle dans le navigateur, carte Monde 2000×1000, 73 nations, début de partie : fichier complet de 29 858 octets. Des frontières réelles, plus découpées qu'un Voronoï, coûteront davantage ; un deflate par-dessus reste possible en v2 via migration.
- **Stockage : interface `SaveStore`** ; `IndexedDbSaveStore` (deux object stores : métadonnées / octets) vérifié dans le navigateur, `MemorySaveStore` pour les tests. Pas de `fake-indexeddb` : l'ajouter imposait un `npm install`, interdit.
- **`Rng` propre à Véritable** (`sim/rng.ts`, sfc32, `getState`/`setState`) ; `PseudoRandom` du cœur n'est pas touché. Graine de campagne = `simpleHash(gameID)`. Un test échoue si un fichier de `src/veritable/` appelle le générateur global.
- **Commande joueur du J0 : `set-speed`** (stocke la vitesse dans le calendrier ; la cadence réelle, la pause et les vitesses sont le calendrier du J1). `SimEvent` du J0 : `nation-status-changed`.
- **Placement de nom** : `GameRunner` ne calcule pas `placeName` pour un joueur vivant à 0 tuile (boîte englobante vide).

### Écarts assumés par rapport à DESIGN.md / DATA-SCHEMAS.md

- **`rngState` = 4 × uint32 (sfc32)** au lieu d'une chaîne.
- **Tuiles en section du conteneur `.vsave`** au lieu de `tilesRef: "tiles.bin"` ; l'en-tête porte `tilesInfo { width, height }`.
- **`calendar` = `{ startDate, elapsedGameMinutes, date, speed }`** (`date` dérivée, gardée lisible) au lieu de `{ date, speed }`.
- **Noms de nations en littéraux jusqu'au J1.** Pas de scénario (J1) ni de fiches (J2) : les nations du J0 viennent du roster du cœur (`scenarioFromCoreGame` : joueur humain → `player`, nations du manifeste de carte → slug du nom). Extension du schéma `scenario` : `adHocNations[{ id, literalName }]`, à retirer quand le chargeur de scénario existe. `regime` est `null` pour ces nations.
- **Rotation des 6 sauvegardes automatiques reportée au J1** (avec le `Scheduler`). Le J0 ne fait que des sauvegardes manuelles ; `SaveMeta.kind` prévoit déjà `auto`.
- **État en vol non sauvegardé jusqu'à son remplacement** : attaques, bateaux, ogives en vol, état interne de l'IA héritée, alliances et embargos du cœur (diplomatie : J3a), relations, statistiques, structures en construction (sauvées comme achevées). Conséquence observée : au tick de restauration l'IA héritée réengage aussitôt des troupes ; l'identité octet pour octet est garantie sur l'état sauvegardé (tuiles, nations, calendrier, Rng, état du joueur), pas sur les troupes des nations IA une fois ce tick passé.
- **Doomsday Clock forcé à off côté client** (mécanique d'élimination), sans toucher au cœur.
- **Tribus (bots) forcées à 0 en solo** — décision prise en session, non prévue au plan. Ce ne sont pas des nations : leurs tuiles n'appartiendraient à personne dans une sauvegarde et disparaîtraient au rechargement. Dans le cœur, un bot meurt toujours à 0 tuile, même en mode Véritable.
- **`maxTimerValue` et `overtime` retirés de la configuration solo** côté client, en plus de la neutralisation dans le cœur.
- **`gameMinutesPerTick = 72`** dans `config.json` (1 mois de 30 jours = 43 200 min par minute réelle = 600 ticks). Le calendrier réel ayant 365 jours, une année dure 12,17 min à ×1 : à régler au J1 avec le calendrier. `defaultStartDate = "2026-01-01"` y vit aussi.
- **Pas de `perf.mark`** au J0 (tâche du J1).

### Modifications de `src/core`

Toutes commentées `// VERITABLE:`, testées dans `tests/veritable/CoreNeutralization.test.ts` et `src/veritable/adapters/VeritableSession.test.ts`.

| Fichier                                                                    | Modification                                                                                                                                               | Raison                                                                                                  |
| -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `src/core/Schemas.ts`                                                      | `veritable: z.boolean().optional()` en fin de `GameConfigSchema`                                                                                           | Drapeau unique                                                                                          |
| `src/core/configuration/Config.ts`                                         | `isVeritable()` ; `overtimeConfig().enabled` forcé à `false`                                                                                               | Overtime neutralisé (lu aussi par `NationMIRVBehavior`)                                                 |
| `src/core/GameRunner.ts`                                                   | `WinCheckExecution` non enregistré ; crochet optionnel `onGameCreated(game)` dans `createGameRunner` ; pas de `placeName` à 0 tuile                        | Ni victoire, ni minuteur, ni plafond 170 min ; attacher la session avant les exécutions                 |
| `src/core/execution/WinCheckExecution.ts`                                  | Garde en tête de `tick()` (lit `gameConfig().veritable`, compatible avec les mocks des tests existants)                                                    | Couvre seuil de victoire, `maxTimerValue`, `HARD_TIME_LIMIT_SECONDS` même si quelque chose l'enregistre |
| `src/core/game/PlayerImpl.ts`                                              | `isAlive()` : un joueur non-bot qui a spawné reste vivant à 0 tuile                                                                                        | Invariant n° 1                                                                                          |
| `src/core/worker/WorkerMessages.ts`, `Worker.worker.ts`, `WorkerClient.ts` | Canal générique `veritable_request` / `veritable_response` / `veritable_events` ; `veritableSave` optionnel dans `init` ; `onCoreTick()` après chaque tick | La sim vit dans le worker                                                                               |

`PlayerExecution.ts` n'a **pas** été modifié, contrairement au plan : sa branche de mort dépend d'`isAlive()`, elle est donc sautée d'elle-même (ni purge de l'or et des unités, ni désactivation). Aucun crochet de restauration n'a été nécessaire dans `Game.ts` / `GameImpl.ts` : l'API publique suffit. Aucun test OpenFront existant n'a été modifié.

### Schémas zod créés (sauvegarde `schemaVersion` 1)

`data/schemas/common.ts`, `nation.ts` (fiche `nations/<iso3>.json`), `scenario.ts` (+ `adHocNations`), `config.ts`, `save.ts` (`SaveHeaderV1Schema`, figé).

## 2026-09-21 — Clôture du J0 (validation du rapport)

- **J0 validé par Lukas**, export → rechargement de page → import d'un `.vsave` vérifié à la main dans le navigateur.
- **Bots (tribus) forcés à 0 en solo : confirmé.**
- **`adHocNations` : accepté comme échafaudage du J0, à retirer au J1** avec le chargeur de scénario.
- **Calendrier réel conservé ; « 1 minute réelle = 1 mois » est une approximation.** La règle exacte est : à ×1, un jour de jeu = 2 secondes réelles (`gameMinutesPerTick = 72`, 20 ticks par jour). Un mois dure 56 à 62 s, une année ≈ 12,2 min (730 s). Exclut un calendrier à mois de 30 jours ou à année de 360 jours. Phrase de DESIGN.md (section « Le temps ») corrigée en conséquence ; les mentions « 1 min = 1 mois » ailleurs (tableau des décisions, ARCHITECTURE, ROADMAP) restent comme raccourci.
- **Règle d'ajout de dépendance** (section Véritable de CLAUDE.md) : `npm install --save-dev <paquet> --ignore-scripts`, puis `npm run inst` ; jamais `npm install` nu. Remplace l'interdiction absolue qui avait empêché d'ajouter `fake-indexeddb` au J0.
- **`fake-indexeddb` ajouté en dépendance de dev** ; `IndexedDbSaveStore` a désormais son test unitaire (`src/veritable/save/IndexedDbSaveStore.test.ts`).

## 2026-09-21 — J1 : on joue (session Claude Code)

### Décisions validées par Lukas avant le code

- **Terres hors scénario : neutres et non conquérables, par un masque de tuiles** (`Game.setUnclaimableTiles`). Le masque vient des frontières du scénario, jamais des propriétaires courants : une tuile qu'une nation perd (retombées nucléaires) reste reprenable ; une tuile possédée n'est jamais neutre, même dans le masque. Écarté : interdire toute attaque sur terre sans propriétaire (une zone irradiée serait devenue imprenable à jamais) ; marquer ces tuiles infranchissables (altère le terrain partagé avec le rendu et le pathfinding).
- **Pas d'IA héritée en campagne.** `NationExecution` n'est pas enregistré en mode Véritable : les nations restent inertes jusqu'au J3a (guerre) et au J5 (IA). Le joueur garde le combat hérité en attendant. Conséquence heureuse : l'identité octet pour octet d'une sauvegarde rechargée tient maintenant aussi dans la durée (testé sur 100 ticks), puisque plus rien ne réengage de troupes au tick de restauration.
- **Sauvegardes du J0 refusées, avec un message explicite** (`save.error.legacy-j0`). Elles décrivent un monde à nations ad hoc que plus rien ne sait recréer. **L'invariant n° 2 (« une sauvegarde antérieure se charge toujours ») court à partir des premières sauvegardes de scénario du J1** ; celles du J0 sont de la préhistoire locale, jamais distribuée. Le format reste `schemaVersion: 1` : la forme de l'en-tête n'a pas changé (le scénario et la nation jouée vivent dans `world.coreStart.config`), aucune migration n'était donc nécessaire.
- **Carte compacte retirée du parcours Véritable** : un scénario est rasterisé pour la carte en taille normale, et le chargeur refuse toute autre grille.
- **Régimes des dix fiches, posés à la main, révisables au J4 avec V-Dem** : FRA, POL, UKR semi-présidentiel ; DEU, GBR, ITA, ESP, NOR parlementaire ; RUS, TUR autoritaire électoral.
- **Champs chiffrés des fiches nations optionnels jusqu'au J2** (`population`, `gdp`, `debtToGdp`, `production`, `military`, `startingTech`, `interestGroups`, `aiAgenda`, `nuclear`) : l'ingestion du J2 les remplit, **et ils redeviennent obligatoires dans le schéma à ce moment-là**. Ce sont des données, pas de la sauvegarde : aucune migration.

### Géoréférencement et frontières (`tools/veritable/borders/`)

- **La carte Europe n'avait aucun géoréférencement** (ni projection ni bornes dans `map-generator` ou `resources`). **Auto-calibration sur le trait de côte**, sans point de contrôle humain : on cherche la projection et le placement qui maximisent le recouvrement (IoU) entre les terres Natural Earth projetées et le masque terre/mer de `map.bin`, du grossier au fin (×8, ×4, ×2, ×1), par recherche par motifs (l'objectif est constant par morceaux). Graine : les nations du manifeste appariées aux points d'étiquette de Natural Earth.
- **Résultat : projection équirectangulaire (plate carrée), IoU 0,9564 à pleine résolution** ; 40,63 tuiles par degré, rapport d'aspect 1,0004, rotation 0,004° ; emprise ≈ 24,7° O – 46,7° E, 30,2° N – 71,3° N. Candidats écartés : LCC 0,79, Albers 0,77, LAEA 0,73, stéréographique 0,72. L'écart à 1 est de l'eau intérieure (lacs, fleuves creusés par OpenFront), pas un décalage. Validé à l'œil par Lukas sur l'image de contrôle. Stocké dans `data/veritable/maps/europe.georef.json`, qui prévoit aussi des `controlPoints` (repli non utilisé) et des `insets`.
- **Source : Natural Earth v5.1.2, 1:10m, version de facto**, domaine public, depuis `github.com/nvkelso/natural-earth-vector` ; sha256 vérifiés à chaque exécution : `ne_10m_admin_0_countries.geojson` `239eec57ac17f100a11e2536cffc56752c318b50ae765b0918ff7aab4ce8f255`, `ne_10m_admin_0_disputed_areas.geojson` `9cafef8b7dfb6b164dc58f218f981f4ace9f716f6c03795d4c62d1ac9f3d50f5`. Cache `tools/veritable/borders/cache/` ignoré par git ; seuls les produits sont commités. Aucune dépendance ajoutée : projections, remplissage de polygones (règle pair-impair, arêtes partagées sans recouvrement ni lacune) et encodeur PNG sont écrits dans l'outil.
- **Règles de rasterisation.** (1) Seules les tuiles terrestres de `map.bin` reçoivent un propriétaire : lacs et fleuves creusés par OpenFront restent de l'eau même où Natural Earth voit de la terre. (2) Tous les pays sont rasterisés, pas seulement ceux du scénario. (3) Les surcharges de facto s'appliquent ensuite. (4) Les côtes orphelines (14 405 tuiles) vont au pays Natural Earth le plus proche **parmi tous les pays**, à 60 tuiles au plus, mer comprise. (5) Le filtre aux nations du scénario ne vient qu'après : une côte belge devient neutre, jamais française.
- **Canaries : encart hors position, laissé neutre.** Les vraies Canaries sont au sud du bord de la carte ; l'encart dessiné en bas à gauche (128 tuiles) est déclaré dans `europe.georef.json` (`insets`, règle `neutral`) et n'est rattaché à personne. Le champ survit à une recalibration.
- **Kaliningrad revient à la Russie** sans règle particulière (multipolygone RUS de Natural Earth). Chypre-Nord (`CYN`) n'est pas la Turquie : neutre au J1.
- **`data/veritable/borders/overrides/<carte>.geojson` est le mécanisme général des territoires contestés.** Une surcharge ne réattribue que les tuiles dont le propriétaire Natural Earth figure dans `from`, si bien qu'un polygone peut déborder sans risque. Au J1 : `crimea` (polygone Natural Earth ; sans effet, la Crimée y est déjà russe ; conservé pour définir la région) et `ukraine-occupied-mainland`.
- **Précision du front ukrainien : approximative, 10 à 30 km.** Tracé à la main par Claude Code d'après sa connaissance de la ligne de contact fin 2025, sans source cartographique importée, marqué `status: "approximate"` et « à valider à la main » dans le fichier ; validé à l'œil par Lukas le 2026-09-21, avec vérification que Koupiansk (rive ouest de l'Oskil) reste ukrainienne. Russe : Louhansk presque entier, est de l'oblast de Kharkiv le long de l'Oskil, Siversk, Tchassiv Yar, Toretsk, Pokrovsk, Houliaïpole, rive gauche du Dniepr. Ukrainien : Kramatorsk, Sloviansk, Kostiantynivka, Orikhiv, Zaporijjia, Kherson. Non tracés : poche de Vovtchansk, incursions frontalières. 17 119 tuiles, soit ≈ 86 000 km² pour ≈ 89 000 réels hors Crimée.
- **Produits** : `borders/europe-10.bin` (76 294 octets ; format `VBRD` = en-tête + liste des nations + bloc RLE du même codec que la sauvegarde), `europe-10.report.json` (tuiles par nation), `europe-10.meta.json` (tuile de chaque capitale). Dix nations : 1 331 874 tuiles sur 2 345 907 terrestres ; 1 013 905 neutres.

### Scénario, chargeur, fiches

- **Un nouveau monde est une restauration.** `initialWorld()` exprime le premier jour du scénario comme un état de sauvegarde (tuiles des frontières, une ville OpenFront sur chaque capitale, qui sert de `spawnTile`) et passe par `CoreBridge.restore()`, le chemin éprouvé au J0. La phase de spawn se clôt au même tick, sans clic. **L'artefact « exilé dès le départ » a disparu** : la sim voit les frontières avant même le premier tick du cœur (testé, y compris sur la vraie carte Europe : dix nations `active`, journal réduit à `campaign-started`).
- **`adHocNations`, `scenarioFromCoreGame` et les noms littéraux sont supprimés.** `scenario.borders` devient obligatoire. Les noms de nations sont des clés i18n (`nation.<iso3>.name`).
- **Écart de DATA-SCHEMAS.md : `capital` = `{ name, lon, lat, source, asOf }`** au lieu de `{ tileHint }`. Une capitale est une donnée géographique ; sa tuile dépend de la carte et c'est l'outil de frontières qui la calcule (`<scénario>.meta.json`), ramenée à la tuile de la nation la plus proche. Ajout : `regimeSource`.
- **Roster du cœur** : une nation du scénario = un joueur de type `Nation` créé par `coreRoster()`, sauf celle du joueur, incarnée par le joueur humain. Liaison nation ↔ joueur par le nom affiché (unique dans un scénario). `GameConfig` porte `veritableScenario` et `veritablePlayerNation`.
- **Chargement des données** : `src/veritable/data/catalog.ts` (`import.meta.glob`, validation zod au premier accès). La sim ne l'importe jamais : elle reçoit les fiches par injection, pour qu'un runner hors Vite (headless, J2) lise les mêmes fichiers sur disque. Le `.bin` est importé en `?inline` (le worker tourne depuis une URL `blob:`, où les URL relatives ne se résolvent pas) ; d'où `assetsInclude: ["**/data/veritable/**/*.bin"]` dans `vite.config.ts`.
- **Guerre en cours** : `rus-ukr-2022` figure dans le scénario comme donnée, sans effet en jeu avant le J3a.

### Temps

- **`Scheduler` central sans état** (`sim/scheduler.ts`) : tout se dérive de `elapsedGameMinutes`, qui est sauvegardé ; le résultat ne dépend pas du découpage du temps (testé tick par tick contre un seul appel). Jour : `economy`, `events`, `diplomacy` ; semaine (tous les 7 jours depuis le départ) : `politics` ; mois (le 1er du mois calendaire) : `politics`, `blocs`, `save`. Les six domaines sont enregistrés et vides. Nouveaux `SimEvent` : `day-started`, `month-started`.
- **Instrumentation par domaine derrière une sonde injectée** (`PerfProbe`) : la sim ne touche pas à `performance`. Dans le worker, `PerformanceProbe` pose un `performance.mark` / `measure` `veritable:<domaine>:<horloge>` par appel, les efface aussitôt (une campagne dure des heures) et cumule appels, total et maximum, lisibles par la requête `perf`.
- **Vitesses = cadence du tick OpenFront.** Pause, ×1, ×2, ×5 pilotent la boucle de tours solo de `LocalServer` (`PauseGameIntentEvent`, `ReplaySpeedChangeEvent(1 / vitesse)`) : le militaire et le calendrier accélèrent ensemble, la sim avance toujours de 72 min par tick. La vitesse est sauvegardée et réappliquée au chargement.
- **Sauvegardes automatiques** : sur `month-started`, le client prend un snapshot et l'écrit avec `kind: "auto"` et l'identifiant `auto-<date de jeu>` (repasser par le même mois écrase son emplacement). `writeAutosave` garde les `save.autosaveSlots = 6` plus récentes (constante dans `config.json`) et ne touche jamais aux sauvegardes manuelles.

### Mesure de ×5 sur la carte Europe (2026-09-21)

- **Navigateur (build de dev, panneau intégré de l'application Claude, carte Europe 2904×1672, dix nations) : ×1 = 9,99 ticks/s, ×2 = 20,09, ×5 = 49,95**, pour des cibles de 10, 20 et 50. Pause : 0.
- **Avant correction, ×5 plafonnait à 41,7 ticks/s** (×1 = 9,5 ; ×2 = 18,7) : ce n'était pas la charge mais la boucle de tours solo de `LocalServer`, qui repartait de « maintenant » à chaque tour et ajoutait ainsi la granularité de son interrogation (≈ 5 ms) à chaque intervalle. **Correction, en campagne uniquement (`// VERITABLE:` dans `src/client/LocalServer.ts`) : planification sans dérive** (`turnStartTime += intervalle`, avec au plus un intervalle de rattrapage pour qu'un blocage ne soit pas suivi d'une rafale). C'est une retouche de la boucle de tours héritée, limitée au mode campagne ; sans elle « un jour = deux secondes » était faux de 5 % à ×1 et de 17 % à ×5. Les tests `LocalServer*` existants restent verts.
- **Banc headless (`VERITABLE_BENCH=1 npx vitest src/veritable/adapters/fiveYears.bench --run`) : cinq ans de jeu, 2026-01-01 → 2031-01-01, soit 36 520 ticks, cœur + sim, sans rendu.** Tick moyen 0,021 ms, médiane 0,017, p99 0,077, maximum 5,4 ms, pour un budget de 20 ms à ×5. Chargement du scénario (1 331 874 tuiles posées, dix villes) : 672 ms. Sauvegarde : 95 ms, 77 004 octets dont 76 235 de tuiles. Rechargement : 679 ms, octets identiques. 60 mois, 260 semaines, 1 826 jours déclenchés ; les sept horloges de domaine (vides) cumulent 1,2 ms sur les cinq ans. Ces chiffres valent pour des nations inertes : ils serviront de base de comparaison au J2.
- **Limite de la mesure navigateur** : le panneau intégré bride ses timers quand il n'est pas interrogé (0 image/s mesurée hors interaction). Les cadences ci-dessus sont mesurées pendant des fenêtres de 8 à 12 s ; les cinq ans de jeu d'une traite ont été joués headless, et dans le navigateur sur 17 mois de jeu (16 sauvegardes automatiques écrites, 6 conservées).

### Modifications de `src/core` au J1

Toutes sous le drapeau `veritable`, commentées `// VERITABLE:`, testées avec un contrôle sans drapeau dans `tests/veritable/NeutralLand.test.ts` et `src/veritable/adapters/VeritableSession.test.ts`.

| Fichier                                 | Modification                                                                                                                                                                                            | Raison                                                               |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| `src/core/Schemas.ts`                   | `veritableScenario`, `veritablePlayerNation` (optionnels) dans `GameConfigSchema`                                                                                                                       | Le scénario et la nation jouée recréent le jeu cœur d'une sauvegarde |
| `src/core/game/Game.ts`, `GameImpl.ts`  | `setUnclaimableTiles(mask)` / `isUnclaimable(tile)` ; additif, refusé hors campagne, taille vérifiée                                                                                                    | Terres neutres (pré-autorisation reconduite)                         |
| `src/core/game/PlayerImpl.ts`           | `canAttack` refuse une tuile neutre et ne traverse pas de terres neutres pour atteindre une cible                                                                                                       | Idem                                                                 |
| `src/core/execution/AttackExecution.ts` | Une attaque sur terre sans propriétaire saute les tuiles neutres (conquête et front)                                                                                                                    | Idem                                                                 |
| `src/core/game/TransportShipUtils.ts`   | Pas de débarquement sur un rivage neutre                                                                                                                                                                | Idem                                                                 |
| `src/core/GameRunner.ts`                | `createGameRunner(…, veritable?: { nations, onGameCreated })` : roster du scénario à la place du manifeste (remplace le paramètre `onGameCreated` du J0) ; `NationExecution` non enregistré en campagne | Chargeur de scénario ; nations inertes                               |
| `src/core/worker/Worker.worker.ts`      | Charge la campagne (`loadCampaign`) avant de créer le jeu                                                                                                                                               | Roster et frontières viennent du scénario                            |

`TransportShipExecution.ts` n'a pas eu besoin d'être modifié, contrairement au plan : il passe par `targetTransportTile`, où vit la garde. Aucun test OpenFront existant n'a été modifié.

### Schémas zod créés ou modifiés (sauvegarde : toujours `schemaVersion` 1)

`nation.ts` (capitale géographique, `regimeSource`, champs chiffrés optionnels jusqu'au J2), `scenario.ts` (`adHocNations` supprimé, `borders` obligatoire, au moins une nation), `config.ts` (`time.defaultStartDate`, `save.autosaveSlots`), nouveau format binaire `data/bordersFile.ts` (`VBRD` v1).

## 2026-09-21 — Clôture du J1 (validation du rapport)

- **J1 validé par Lukas.** Correction de dérive de `LocalServer` validée.
- **Le bouton « Solo » hérité ouvre le panneau de départ Véritable** au lieu de lancer une campagne France par défaut (`SinglePlayerModal.open()` redirige vers le panneau, `// VERITABLE:` ; d'abord fait dans `GameModeSelector`, déplacé le 2026-09-22 parce que deux tests OpenFront vérifient que la carte Solo ouvre bien la modale sans être bloquée). Suppression du bouton au J7. La modale solo d'OpenFront n'est plus atteignable que par le tutoriel.
- **Chypre-Nord reste neutre au J1. À traiter au J6 comme région contestée de facto distincte de la Turquie** (Natural Earth la donne comme entité à part, `CYN`) : contrôleur de facto à modéliser, reconnue par la seule Turquie.
- **Dette n° 1 — catalogue de données.** `src/veritable/data/catalog.ts` repose sur `import.meta.glob` (Vite) ; un runner lancé par `tsx` ne peut pas l'importer. Réglée au J2 par un chargeur à deux implémentations (Vite et `fs`) derrière une même interface.
- **Dette n° 2 — liaison nation ↔ joueur du cœur par nom d'affichage** (`bindScenario`) : deux nations d'un même scénario ne peuvent pas partager un nom, et une traduction change la liaison. À remplacer par un identifiant (la `NationId` portée par le `PlayerInfo`) la prochaine fois qu'on touche le cœur, au plus tard au J3a.

## 2026-09-22 — J2 : économie et noyau politique (session Claude Code, sans validation intermédiaire)

Plan et écarts : `docs/veritable/plans/J2.md`. Formules : celles de la consigne du 2026-09-21 ; ce qui suit consigne les choix qu'elle laissait ouverts.

### Marché

- **Participants du marché = les dix nations + `ROW`.** Le reste du monde n'est pas une entrée de `nations[]` : hors carte, jamais jouable, sans politique ni budget, par construction. Production et consommation par bien = totaux mondiaux moins les dix. **Il ferme le bilan mondial** : sa consommation = production mondiale − consommation des dix, si bien que les écarts statistiques entre séries de production et de consommation (charbon : 10 %) ne se retrouvent pas dans le prix. Distance fixe 4 000 km, élasticités doubles, croissance tendancielle 2,5 %/an, choc d'offre AR(1) par bien.
- **Unités : énergie en TWh par an** (pétrole, gaz, charbon, électricité), l'unité dans laquelle Our World in Data publie les quatre séries ; prix en M$/TWh (= $/MWh). Écart assumé par rapport aux exemples « Mbbl, bcm » : aucune conversion estimée. Alimentation en Mt de céréales (Banque mondiale / FAO). Les sept autres biens en **indice 100 = production des dix nations**, avec un `basePrice` en M$ par point calculé à l'ingestion, pour que droits de douane et rentes aient une assiette en dollars.
- **Embargo et prix.** Avec un prix mondial unique et un reste du monde qui absorbe tout, un embargo ne changerait que des parts de flux. Règle retenue : le surplus qu'un embargo empêche de livrer est revendu au reste du monde avec la décote (`sanctionDiscount` = 0,2, sur l'assiette des rentes) **et sort de l'offre qui forme le prix** (`S` effectif = `S` − volume bloqué). Le prix monte, la couverture des importateurs chute, puis l'offre élastique referme l'écart. C'est la lecture de DESIGN.md (« perd ses exportations […] et revend à prix décoté ailleurs ») qui rend la coupure visible. Le contournement vient au J3a.
- **Électricité.** Part fossile de la production (données OWID, par combustible) × couverture du combustible le mois précédent ; l'industrie (acier, biens de consommation, électronique, armement, pharmacie) perd `electricityShortageOnIndustry` = 0,5 de sa production par point d'électricité manquante. Échanges seulement entre nations partageant une frontière terrestre sur la carte (adjacences calculées par l'outil de frontières) ; le reste du monde est voisin de toute nation qui touche une terre neutre.
- **Pénuries.** Couverture = (production propre + imports) / besoin. Indice = Σ poids du bien × (1 − couverture), poids dans `goods.json`. L'alimentation compte double dans la stabilité (`foodShortageWeight`). `armsShort` est posé pour les divisions du J3a, sans effet au J2.
- **Aléa** : bruit de croissance mensuel par nation (σ = 0,15 %) et choc d'offre du reste du monde, tirés du `Rng` : c'est ce qui distingue les graines.

### Budget, politique, IA

- **Calibrage du budget.** Taux d'imposition calculés pour que chaque impôt rapporte sa part des recettes observées (Banque mondiale) ; un taux qui dépasserait son plafond (assiette minuscule) est écrêté et le manque passe en TVA. Assiettes : revenu 50 %, sociétés 15 %, TVA 55 % du PIB ; droits de douane sur la valeur des imports ; rentes sur la valeur de la production des biens `rent`.
- **Taux d'intérêt réel** : `i = 0,02 + 0,01 × max(0, dette/PIB − 0,6) + 0,02 × (1 − stabilité)`. Première calibration (0,03 / 0,04) abandonnée : avec un PIB réel sans inflation elle donnait 5,8 % à l'Italie et un défaut en 2058.
- **Austérité forcée** : postes plafonnés à 90 % de leur part du premier jour (et non de leur part au moment du déclenchement : pas d'état supplémentaire). **Défaut** : décote 50 %, postes ramenés aux recettes pendant 5 ans, −0,15 de satisfaction et de stabilité.
- **Curseurs à effet immédiat** (au mois suivant). L'effet progressif (`rampMonths`) est une tâche du J4.
- **Croissance** : `alpha` = 0,02 par mois et par point de PIB d'investissement au-dessus de la référence (infrastructures + recherche du premier jour) ; première valeur 0,05 abandonnée : l'austérité de l'IA faisait reculer le PIB italien de 25 % en 50 ans.
- **Règle budgétaire IA (`ai/fiscal.ts`), pas l'IA du J5.** Le PIB étant réel, un déficit de 3 % laisserait la dette monter sans fin : la règle consolide si le déficit dépasse 3 % **ou** si la dette dépasse 60 % du PIB et monte depuis 12 mois ; elle épargne infrastructures et recherche ; elle relâche vers les valeurs du premier jour, jamais au-delà. En headless (`autopilot`) elle pilote aussi la nation « du joueur ».
- **Asymétrie** : huit groupes pour la nation du joueur seulement ; les autres ont une opinion proxy (croissance, pénuries, prix). Poids des groupes : défaut dans `config.json`, surcharge possible par `interestGroups` dans la fiche.
- **Règle UE** dans `blocs/eu.json` (`fiscalRule`) : malus d'opinion de 0,03 tant que dure le dépassement, entrée au journal à son début. Bonus commercial de bloc (`tradeBonus` = 1,5) entre membres pleins.

### Données et ingestion

- **Sources épinglées** : Banque mondiale (CC BY 4.0), 14 indicateurs, **instantanés JSON commités** (l'API n'est pas versionnée) avec sha256 du JSON canonique dans `tools/veritable/ingest/sources.lock.json` — indépendant du formatage, Prettier repassant sur les fichiers commités ; Our World in Data énergie (CC BY 4.0), commit `7e387a16`, CSV en cache hors git, sha256 vérifié.
- **Céréales lues à l'année 2023** : l'agrégat mondial 2024 de la Banque mondiale est partiel (1 644 Mt contre 3 124).
- **Croissance tendancielle** : moyenne 2015-2024 de la croissance réelle, bornée à [1 %, 4 %] (Ukraine, Allemagne, Turquie écrêtées, la note de la fiche le dit).
- **Estimations (`source: "estimate"`, justifiées dans `tools/veritable/ingest/estimates.json`)** : dette publique des dix (périmètre Maastricht ; la série Banque mondiale est lacunaire et de périmètre « administration centrale ») ; dons extérieurs de l'Ukraine (26 % du PIB) ; ventilation du résidu de dépenses (social 65 %, infrastructures 20 %, subventions 15 %) et part publique de la R&D (35 %) ; répartition des recettes par impôt ; consommation alimentaire (population × production mondiale par habitant) ; minerais critiques (parts, indice mondial, valeur) ; parts de la valeur ajoutée manufacturière par bien en indice et part d'équipement des dépenses militaires ; effectifs et puissances aérienne / navale ; ogives et doctrines ; prix de base et élasticités des biens (dans `goods.json`).
- **À vérifier avant distribution** : `MS.MIL.XPND.GD.ZS` (dépenses militaires) est republié par la Banque mondiale mais sa source primaire est le SIPRI.
- **`DataSource` à deux implémentations** (`files.vite.ts`, `files.fs.ts`) derrière une même validation : règle la dette n° 1. La sim ne reçoit que des objets validés (`SimData`).
- **Champs des fiches de nouveau obligatoires** : `population`, `gdp`, `debtToGdp`, `economy`, `military`, `nuclear` (nullable), `startingTech`. Restent optionnels par conception : `interestGroups` (surcharge) et `aiAgenda` (J5). Écart de DATA-SCHEMAS.md : `production: { bien: ratio }` devient `economy.goods.<bien>.{production, consumption}` en quantités sourcées.

### Sauvegarde

- **`schemaVersion: 2`** : sections `economy` (marché, reste du monde, embargos, économie de chaque nation) et `politics`. Le schéma v1 est figé dans `data/schemas/saveV1.ts` ; `save.ts` est la version courante.
- **`migrations/v1-to-v2.ts`** : une campagne v1 n'avait pas d'économie ; les deux sections partent donc des fiches, comme une nouvelle campagne, quelle que soit la date de la sauvegarde. Une migration reçoit désormais un **contexte** (`MigrationContext` : config, `SimData`, fiches). Testée sur une vraie sauvegarde J1 (`save/fixtures/j1-europe-10.vsave`, Pologne, 15 mars 2026) et vérifiée dans le navigateur sur une sauvegarde J1 faite au jalon précédent.
- `peekCoreStart()` lit le `GameStartInfo` d'une sauvegarde de n'importe quelle version sans la migrer (l'interface en a besoin avant que le worker existe).

### Runner headless v1

- `npm run veritable:headless -- --scenario europe-10 --years 20 --runs 10 --seed 42 [--shock cut-gas-exports:RUS@2028-01] [--player NAT] [--out dir]` : JSON par campagne (prix finaux et extrêmes, dette, stabilité, événements, défauts, troubles, temps CPU par domaine et par horloge), CSV mensuel, `summary.json`.
- **Sans le cœur OpenFront au J2** (`adapters/BordersWorld.ts`, monde statique sur les frontières du scénario) : l'économie ne dépend des tuiles que par les adjacences. Le cœur entre dans le runner au J3a. 20 ans ≈ 0,35 s par campagne.
- **Résultats de clôture** : 20 ans × 10 graines (42 à 51) : prix entre 0,95 et 1,04 × base, dette/PIB maximale 1,46 (Italie, en baisse ensuite), aucun défaut, aucun trouble. 50 ans × 3 graines : prix entre 0,93 et 1,07, aucun défaut. Choc gazier contre témoin : `docs/veritable/reports/J2/`.
- Temps CPU sur 20 ans : prix journaliers ≈ 140 ms, flux et PIB mensuels ≈ 100 ms, politique hebdomadaire ≈ 17 ms, budget ≈ 6 ms.

### Deux pièges d'environnement rencontrés à la clôture

- **`npm run build-prod` laisse un dossier `static/` (ignoré par git) qui fait échouer `tests/server/RenderHtml.test.ts`** : le test attend un manifeste d'assets vide et le rendu lit celui du build. Le build de vérification du J1 l'avait laissé derrière lui. Règle : après un build de vérification, supprimer `static/` avant `npm test`.
- **Prettier lancé sur un dossier entier reformate des fichiers qu'on ne touche pas** (`DESIGN.md`, `ARCHITECTURE.md`, instantanés d'ingestion). Règle : ne formater que les fichiers modifiés ; les empreintes des instantanés portent sur le JSON canonique.

### Schémas zod créés ou modifiés

`goods.ts`, `row.ts`, `bloc.ts` (nouveaux) ; `nation.ts` (économie obligatoire) ; `config.ts` (`economy`, `budget`, `politics`, `ai`) ; `save.ts` (v2) et `saveV1.ts` (figé). `src/core` n'a pas été touché au J2.

## 2026-09-22 — Clôture du J2 (validation du rapport)

- **J2 validé par Lukas.** Corrections demandées : PIB avec terme d'exportation, substitution différée du reste du monde, règle UE révisée, dette FMI si la licence le permet, runner sur le cœur ; l'absence de troubles des nations IA attend le J4.
- **Observations sans action** : prix trop calmes sur vingt ans (les chocs viendront des guerres et des événements) ; dette négative de la Norvège (le taux négatif et l'absence d'austérité se comportent).
- **Liaison nation ↔ joueur du cœur par identifiant** demandée pour la première retouche du cœur : faite au J3 (dette n° 2 du J1 réglée).

## 2026-09-22 — J3 : guerre terrestre, réaction internationale, marine, air, logistique (session Claude Code, sans validation intermédiaire)

Plan et écarts : `docs/veritable/plans/J3.md`. Formules : celles de la consigne du 2026-09-22 ; ce qui suit consigne les choix qu'elle laissait ouverts et les calibrations.

### Corrections du J2

- **Terme d'exportation** : `g += delta × (X/PIB − part de référence)`, `X` = exportations réellement vendues (livrées au prix mondial, invendus revendus avec la décote). La référence est une **part de PIB adaptative** (τ = 60 mois, `exportReferenceAdaptMonths`) et non la valeur figée de 2026 : figée en dollars, elle divergeait avec la croissance des exportations ; figée en part, elle pesait à jamais après un choc. `delta` = 0,15.
- **Substitution différée** : `rowEffectiveProduction` rejoint sa cible (production × réponse au prix) avec τ = 18 mois ; la demande du reste du monde reste élastique instantanément. C'est ce qui forme le prix : le reste du monde n'a plus d'élasticité d'offre instantanée.
- **Prime régionale** (`scarcityPremium` = 3,5) : prix à l'importation = prix mondial × (1 + 3,5 × part non couverte de la demande du scénario). C'est le « paie une prime ailleurs » de DESIGN.md ; un embargo qui retire 3 % de l'offre mondiale ne peut pas faire +25 % sur un prix mondial unique (il fait +5 %). **Le critère « +25 % » est mesuré sur le prix à l'importation**, le prix mondial figure à côté dans les CSV (`import_price_<bien>`). Six passes de rationnement (au lieu de trois) pour que la couverture de base soit à 100 % et la prime résiduelle nulle en temps normal.
- **Décote de sanction** portée à 0,4 puis 0,5 (décote de l'Oural en 2022-2023).
- **Règle UE révisée** : réprimande si le déficit glissant sur douze mois dépasse 3 % pendant deux années consécutives (`deficitYears`), ou si la dette dépasse 60 % et monte depuis douze mois ; malus 0,03 en vigueur, qui décroît de 0,005 par mois une fois la condition levée (`malusFadeMonths` = 6). État : `deficitBreachMonths`, `reprimandMalus` (v3). Une réprimande du J2 conservée par une sauvegarde migrée garde son malus plein et est réévaluée le mois suivant.
- **Dette publique : FMI WEO** (`GGXWDG_NGDP`, API DataMapper, valeur 2025 = estimation du FMI). La page des conditions d'utilisation du FMI est servie aux navigateurs seulement (407 octets pour l'outil) : impossible de vérifier la licence de redistribution. **Décision : le jeu de données n'est pas redistribué** — cache hors git (`tools/veritable/ingest/cache/`), sha256 dans le verrou, `npm run veritable:ingest -- fetch-imf` ; seules dix valeurs attribuées (`source: "imf-weo:GGXWDG_NGDP"`) figurent dans les fiches. Écarts avec les estimations de mémoire : UKR 0,95 → 1,087 ; RUS 0,20 → 0,172 ; NOR 0,43 → 0,45 ; POL 0,58 → 0,588 ; FRA 1,13 → 1,16. L'entrée `debtToGdp` d'`estimates.json` reste un repli pour une nation absente de la série.
- **Liaison par identifiant** : `PlayerInfo.nationId` (cœur, additif, dernier paramètre du constructeur) ; l'humain le reçoit de `GameConfig.veritablePlayerNation` dans `createGameRunner`, le roster du scénario de la fiche. `bindScenario` accepte un humain sans identifiant (tests, anciens salons).
- **Fixture `save/fixtures/j2-europe-10.vsave`** : vraie sauvegarde v2 (Allemagne, 10 mars 2026, ×2, TVA 30 %, embargo RUS → DEU gaz) produite par le code du J2 sur la carte Europe avant tout changement de schéma ; la migration v2 → v3 est testée dessus.

### Sauvegarde v3

- **Une seule nouvelle version pour tout le jalon** : `schemaVersion: 3` a pris forme au fil des commits (économie, diplomatie, militaire, marine) ; `saveV2.ts` est figé, `save.ts` est la version courante, `migrations/v2-to-v3.ts` reçoit le contexte (fiches, scénario) et construit diplomatie, militaire et marine comme une nouvelle campagne ; l'économie d'une sauvegarde v2 est conservée. Une sauvegarde v3 d'un commit intermédiaire du jalon n'est pas supportée : aucune n'a existé hors de la machine de développement.
- **Bit 12 de la tuile sauvegardée = « contesté »** (`TILE_CONTESTED_BIT`) : terre prise par la guerre ou cédée à la paix. L'index de nation garde ses douze bits, les retombées le bit 13. `CoreBridge` porte le masque ; le cœur ne le connaît pas.

### Diplomatie et réaction internationale

- **Relations** `[a][b]` pour `a < b`, initialisées à +30 par bloc commun d'après les étiquettes de bloc des fiches (`blocs: ["eu", "nato", "g7", "g20"]`), plafond 60, −100 entre belligérants, retour vers 0 à +1 par mois.
- **Le coût d'une déclaration s'applique à la déclaration et chaque mois de guerre** (Δ = coût du casus belli × (1 + part de l'agresseur dans la puissance totale)). Lecture retenue parce qu'avec un coût unique, un agresseur parti de +60 avec ses voisins de bloc ne passait jamais sous −40 : aucune sanction n'était possible et le test de livraison 2 (cinq sanctions en six mois) ne tenait pas. Une guerre d'agression coûte tant qu'elle dure.
- **Guerres du scénario** (`rus-ukr-2022`) : actives dès le premier jour, agresseur = premier camp, `declaredInCampaign: false` : ni coût mensuel de relations ni coalition, le monde les a déjà « payées ». Seule la victime sanctionne son agresseur (règle générale : un défenseur sanctionne toujours l'agresseur qui le combat). Les sanctions initiales du monde de 2026 (UE, G7 contre la Russie) sont une donnée du J6, validée à la main.
- **Sanctions IA** évaluées au mois avec la diplomatie (l'horloge quotidienne du domaine ne porte que la marine) : relations < −40 et (bloc commun avec une victime, ou agresseur > 25 % de la puissance totale). **Alignement de bloc (couche 1)** : seules les entités de bloc (`blocs/*.json`, l'UE au J3) alignent leurs membres pleins quand la moitié sanctionnent ; les étiquettes de bloc des fiches (OTAN, G7, G20) ne servent qu'aux relations et à « bloc commun ». Sanctions levées quand la nation n'est plus agresseur nulle part et que les relations sont revenues au-dessus de −40.
- **Coalition** : ouverte contre un agresseur sans casus belli qui pèse plus de 1,5 fois ses victimes, pour toute nation IA à moins de −60 avec lui ; fenêtre de trois mois, probabilité mensuelle 0,3 tirée du `Rng` ; le rejoignant entre côté défenseurs, à −100 avec les agresseurs, sans coût de relations.
- **Puissance militaire** = Σ divisions ((attaque + défense)/2 × effectif × équipement × entraînement) + 20 × puissance aérienne + 20 × puissance navale de la fiche (`war.power`).
- **Casus belli** (`data/veritable/war/casus-belli.json`) : `contested-territory` (la cible contrôle une région du scénario ou une cession antérieure revendiquée par le déclarant), `ally-attacked` (la cible est l'agresseur d'un membre plein d'un bloc commun), `humanitarian` (cible en troubles, stabilité < 0,3), `none` (toujours disponible, coût 15 au lieu de 5). La vue du joueur liste les casus belli disponibles contre chaque nation.
- **Les jeunes et les entrepreneurs de l'agresseur perdent 0,1** à la déclaration ; l'épuisement pèse ensuite sur tous les groupes et sur le proxy IA (`exhaustion.opinionWeight` = 0,3).

### Guerre terrestre

- **Frontière sim ↔ monde étendue** (`WorldPort`) : `fronts(paires, segmentTiles)`, `advance(front, segment, gagnant, perdant, n)`, `transferAll`, `naval()`, `landingZone`, `launchLanding`. La sim raisonne en segments ; la géométrie est calculée par le monde (`sim/war/geometry.ts`, fonctions pures sur des accesseurs, partagées par `CoreBridge` et le monde mémoire des tests).
- **Géométrie** : le front = tuiles des deux belligérants qui se touchent (4-voisinage) ; composantes connexes (8-voisinage) parcourues d'un bout à l'autre et découpées en segments d'environ 200 tuiles (`segmentTiles`), dans l'ordre de la ligne. **Recalculée une fois par jour de jeu** (et après toute commande ou événement qui change les guerres) : les captures de la journée s'appuient sur les segments lus le matin. Conséquence assumée : une campagne rechargée relit les fronts sur les tuiles courantes, ses captures peuvent différer de la campagne sauvegardée jusqu'au jour suivant ; l'identité octet pour octet est garantie au rechargement immédiat et sur des avances par jours entiers (testé). Le recalcul à chaque tick, qui rendrait l'identité parfaite, coûtait ~1 ms par tick et par front actif et faisait exploser les tests ; refusé pour l'instant.
- **Capture par mécanique propre**, pas par `AttackExecution` : celui-ci est couplé aux troupes héritées, au ratio d'attaque et à la conquête totale sous 100 tuiles (`handleDeadDefender`), trois choses que Véritable retire. `captureAlong` prend d'abord les tuiles du perdant qui ont le plus de voisins du gagnant, puis les voisins de ce qui vient d'être pris ; `Player.conquer` du cœur fait le reste.
- **Vitesse** : `v = min(vMax, v0 × (r − 1))` tuiles par tick et par segment, fraction tirée du `Rng` (pas d'accumulateur à sauvegarder). Première calibration `v0` = 0,5, `vMax` = 3 : la France prenait 64 % de l'Espagne en deux ans. Retenu **`v0` = 0,1, `vMax` = 1** : ~10 % en trois ans avec dix divisions blindées de plus, et l'Ukraine cède du terrain lentement.
- **Pertes** : `lambda` = 0,7 homme par tick et par point de force adverse, seulement sur les segments où quelqu'un attaque ; l'équipement baisse dans la même proportion que l'effectif. Percée : vitesse × 1,5, pertes × 1,3.
- **Terrain** : plaine 1, colline OpenFront tenue pour forêt (×1,2), montagne ×1,5 ; multiplie la défense. **Structures** : poste de défense = `defensePostDefenseBonus` du cœur (5) sur la part des tuiles à portée, ville = `cityDefense` 1,25 (le cœur n'a pas de bonus de ville) sur `cityDefenseRange` 15 tuiles.
- **Divisions** : gabarits de la consigne dans `war/divisions.json`. Armée de départ = effectifs de la fiche répartis selon `startingMix` (50 % infanterie, 25 % mécanisée, 15 % blindée, 10 % artillerie), niveau de conscription initial = le plus bas dont le plafond contient l'armée (Russie partielle, Ukraine totale). Réserve d'effectifs = plafond − hommes en division, renouvelée de 5 % du plafond par mois ; une division levée part à 50 % d'équipement ; armement : la moitié de ce que la nation a le mois (production × couverture + imports − exports d'armement, en points d'indice) recomplète les divisions au prorata de leur déficit, 0,25 point d'indice par unité d'`arms` du gabarit. Entraînement de la nation = 1 + 0,1 par point de PIB de défense au-dessus du premier jour, dans [0,5 ; 1,5] ; celui d'une division y converge de 10 % par mois.
- **Score de guerre** = tuiles prises × 0,001 + pertes infligées × 0,0001 − pertes subies × 0,0001. **Recul** : nation qui a perdu des tuiles nettes dans le mois.
- **Paix** : les termes s'imposent au destinataire (cessez-le-feu ; cession = les tuiles que l'offrant tient restent à lui et forment une région contestée revendiquée par le perdant ; annexion = toutes les tuiles, la nation passe « exilée », dissolution et baroud au J7 ; réparations en % du PIB sur N ans, versées mois par mois dans le budget ; plafond de divisions, l'excédent dissous). Coût = cession × 0,001 par tuile + réparations × 1 par (% × an) + 0,5 par division au-dessus du plafond + 100 pour une annexion (`war.peace`). L'IA accepte si coût ≤ score de l'offrant et (épuisement > 0,5 ou recul ≥ 6 mois) ; une IA répond à l'instant, le joueur reçoit l'offre (`peace-offered`) et répond par commande. **La paix termine toute la guerre**, pour tous ses belligérants.
- **IA de guerre minimale** (`ai/war.ts`) : divisions réparties sur les segments de ses fronts au prorata de la menace (apportionnement de Hamilton), attaque sur un segment où sa force d'attaque dépasse 1,5 fois la défense adverse, conscription partielle en guerre et totale quand elle recule depuis trois mois ou a perdu 10 % de ses tuiles, lève des divisions tant que la réserve le permet, propose un cessez-le-feu quand elle est épuisée ou recule depuis six mois. Rien d'autre.
- **Événements de commandes** : `record` pousse dans `pending` et l'événement sort avec l'`advance` suivant (le J2 le perdait).

### Marine, air, logistique

- **Zones maritimes** : germes dans `data/veritable/maps/europe.seas.json` (les douze de la consigne + le golfe de Gascogne, sinon ses eaux allaient à la Manche), partition des tuiles d'eau par **parcours en largeur multi-source sur l'eau** (une mer fermée n'appartient qu'au germe atteignable par l'eau ; lacs hors zone), `npm run veritable:borders -- zones --scenario europe-10` → `borders/europe-10.zones.bin` (format `VZON`, 113 ko) et `.zones.json`. La mer de Barents a son germe sur le bord haut de la carte (71,3° N).
- **Présence** dans une zone = puissance navale de la fiche (× couverture en armement) × part de la flotte déployée (par défaut réparties sur les zones côtières et portuaires de la nation) + 0,1 par navire OpenFront + 0,05 par port sur la zone ; **contrôle** = part de la présence, neutres compris. **Blocus** d'une nation = moyenne, sur ses zones côtières et portuaires, du contrôle de ses ennemis ; les nations IA n'ayant pas de port, les côtes comptent avec les ports (écart assumé à « adjacentes à ses ports »). **Effet** : la part perdue des flux qui vont par mer (paires sans frontière terrestre, reste du monde compris) est **un volume perdu en mer**, ni livré ni vendu, retiré de l'offre qui forme le prix — un simple poids sur les affinités ne faisait que déplacer des parts et ne réduisait rien.
- **Débarquement** : commande vers un ennemi ; cible = un de ses ports, sinon sa côte la plus proche de la capitale de l'attaquant ; refusé si le contrôle de la zone **entre belligérants** (les flottes neutres ne comptent pas, sinon personne ne débarque jamais dans une mer partagée) est sous 50 % ; sinon un transport OpenFront part et prend à l'arrivée une tête de pont de 6 tuiles de rayon (`Game.setVeritableLanding`, crochet additif ; `TransportShipExecution` ne lance plus d'attaque héritée en campagne). Les fronts prennent le relais.
- **Air** : `a = air_a / (air_a + air_b)`, multiplicateur de segment 1 + 0,5 × (a − 0,5) ; frappes mensuelles : `strikeDamage` = 0,1 × a du plus fort ennemi, décroît de moitié par mois ; réduit la capacité industrielle (`effectiveProduction`) et la capacité de ravitaillement.
- **Logistique** : capacité d'un segment = 2 + 2 × (ports et villes de la nation à portée, pour l'instant 0 sur le cœur : compté au J3b-bis) + 50 × part d'infrastructures du PIB, × (1 − frappes) ; facteur = min(1, capacité / divisions engagées). Vingt divisions sur un segment nu combattent à 10 %.

### Sanctions non cosmétiques

Le test de livraison 2 a révélé que dans l'économie de flux nets du J2 les exportations ne pèsent que 12 % du PIB de la France (33 % dans la réalité : le commerce brut intra-industriel n'est pas modélisé), et que le reste du monde, acheteur sans fond, absorbait à plein prix ce que les sanctionneurs refusaient : une France sanctionnée par neuf nations perdait 3 % de PIB en deux ans, et une guerre sans casus belli rapportait. Trois mécanismes, tous dans `config.json` :

- **Part de marché perdue et décote** : pour chaque exportateur embargoé, part de ses acheteurs potentiels (affinité × déficit) qui l'embargoent ; ce qu'il vend encore l'est avec la décote au prorata de cette part.
- **Indice de contournement** (`circumvention`, DESIGN.md « contournement et corruption ») : seule la part `initial` = 0,2 (+0,015 par mois d'embargo, jusqu'à 1) du marché perdu est réorientée ; le reste est **retenu** : invendu, écoulé avec la décote, hors de l'offre qui forme le prix. Il redescend une fois l'embargo levé.
- **Dépendance commerciale** : niveau de PIB visé = 1 − 0,5 (`sanctionFriction`) × ouverture commerciale (Banque mondiale `NE.TRD.GNFS.ZS`, nouvel indicateur des fiches) × part des partenaires (pondérés par distance et PIB, reste du monde compris avec son PIB = monde − dix) qui sanctionnent ou combattent la nation × (1 − contournement), atteint en trois mois. Ne concerne que les sanctions et les guerres, pas un embargo par bien.

Résultats (graine 42, sans bruit) : la France sanctionnée par neuf nations perd 9,6 % de PIB à deux ans et 9,0 % à trois ; embargo UE sur le gaz et le pétrole russes : Russie −8,2 % à deux ans (au-dessus du critère « au moins 3 % » ; les leviers pour l'adoucir sont `circumvention.initial` et `sanctionDiscount`).

### Runner headless

- **Le cœur OpenFront entre dans le runner** (`--core`, `tools/veritable/headless/coreDriver.ts`) : même `VeritableSession` que le worker, vraie carte Europe, vingt ticks par jour ; graine = nom de la partie (`headless-<graine>`), la sim en tire sa graine comme dans le jeu. 20 ans ≈ 27 s. Sans `--core`, le monde statique du J2 (`BordersWorld`, sans front) reste disponible.
- **`--script commandes.json`** : commandes joueur datées (validées par `PlayerCommandSchema`) et macro `send-all` ; la nation scriptée n'est plus pilotée par les règles IA. Scripts des tests de livraison dans `tools/veritable/headless/scripts/`.
- **Colonnes CSV ajoutées** : prix à l'import, commerce maritime, blocus, tuiles, épuisement, nombre de sanctionneurs, guerre en cours, part des exportations, contournement.
- **Résultats de clôture (cœur, 20 ans × 10 graines 42-51)** : prix entre 0,93 et 1,07 × base, dette/PIB maximale 1,49 (Italie), aucun défaut, aucun trouble ; la guerre russo-ukrainienne du scénario se termine par un cessez-le-feu au bout de trois ans environ (épuisement > 0,5 des deux côtés) et les sanctions ukrainiennes sont levées ensuite. Tests de livraison : `docs/veritable/reports/J3/`.

### Modifications de `src/core` au J3

Toutes sous le drapeau `veritable`, commentées `// VERITABLE:`.

| Fichier                                          | Modification                                                                                                                        | Raison                                                   |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| `src/core/game/Game.ts`                          | `PlayerInfo.nationId` (dernier paramètre, `null` hors campagne) ; `setVeritableLanding(handler)` / `veritableLanding(player, tile)` | Liaison par identifiant ; tête de pont d'un débarquement |
| `src/core/game/GameImpl.ts`                      | Implémentation du crochet de débarquement, refusé hors campagne                                                                     | Idem                                                     |
| `src/core/GameRunner.ts`                         | L'humain reçoit `veritablePlayerNation` dans son `PlayerInfo`                                                                       | Liaison par identifiant                                  |
| `src/core/execution/TransportShipExecution.ts`   | À l'arrivée en campagne : tête de pont par le crochet, troupes rendues, pas d'`AttackExecution`                                     | Le combat hérité n'existe plus en campagne               |
| `src/client/ClientGameRunner.ts` (client)        | Un clic sur la carte n'envoie ni attaque ni bateau en campagne (accès à la config tolérant aux doublures des tests OpenFront)       | Les fronts et divisions bougent les lignes               |
| `src/client/hud/layers/ControlPanel.ts` (client) | Ratio d'attaque masqué en campagne (la barre de troupes reste : les structures coûtent des troupes)                                 | Idem                                                     |

Aucun test OpenFront existant n'a été modifié. Le test `tests/veritable/CoreNeutralization.test.ts` et les tests de session couvrent les crochets.

### Schémas zod créés ou modifiés (sauvegarde `schemaVersion` 3)

`war.ts` (gabarits, casus belli, postures, conscription), `seas.ts` (germes), `data/zonesFile.ts` (`VZON`), `nation.ts` (`economy.tradeOpenness` obligatoire), `row.ts` (`gdp`), `config.ts` (`diplomacy`, `war`, `naval`, `air`, `logistics`, `economy.circumvention`, `sanctionFriction`…), `bloc.ts` (règle budgétaire étendue), `save.ts` (v3) et `saveV2.ts` (figé).

### Observations et dettes

- **Stabilité d'un agresseur** : la France en guerre tombe de 0,63 à 0,49 puis remonte à 0,545 contre 0,594 pour le témoin à trois ans ; l'épuisement seul ne suffit pas à maintenir la chute, la reprise de l'opinion (référence adaptative des exportations, croissance qui repart) l'efface. À revoir au J4 avec les élections.
- **Pénurie et exportations sans commerce brut** : le modèle de flux nets reste faible pour les nations très intégrées ; la dépendance commerciale est un pont, le commerce brut un chantier du J4 ou du J6.
- **Fronts recalculés une fois par jour** (voir plus haut) ; à mesurer au J6 à 195 nations.
- **Logistique : ports et villes à portée d'un segment non comptés sur le cœur** (`supply` = 0 côté `CoreBridge`) : seule la part d'infrastructures et la base jouent. Reste du J3b.
- **Chypre-Nord**, liaison par nom : dettes du J1 soldées ou reportées comme prévu (Chypre-Nord au J6).
- **Journal** : les offres de paix IA → IA refusées s'accumulent (une par mois par guerre) ; compactage du journal au J6.

## 2026-09-23 — Clôture du J3 (validation du rapport) et corrections

- **J3 validé par Lukas.** Cinq corrections demandées avant le J4, toutes faites le 2026-09-23 :
  1. **Relations : coût unique avec casus belli, érosion mensuelle seulement pour l'agresseur sans casus belli ; défenseurs et membres de coalition ne paient rien ; après la paix, les relations négatives reviennent vers 0 à +2 par mois** (`relationRecoveryPerMonth`), les positives s'érodent toujours à −1. Remplace la règle du J3 (coût chaque mois pour tout agresseur). Tests 2 et 3 du J3 revérifiés sans recalibrage : la France (sans casus belli) paie toujours chaque mois.
  2. **Embargo russe : contournement par bien.** L'indice de contournement vit par bien (`circumvention: { initial, perMonth, max }` dans `goods.json`, défaut dans `config.json`) ; il monte chaque mois en proportion de la part de marché perdue sur ce bien (et non d'un simple « embargoé ou pas » : une sanction ukrainienne isolée ne rendait plus la Russie prête à tout contourner) ; il est plafonné (`max`), et **la décote sur ce qui est réorienté ou écoulé se referme avec lui** (`décote × (1 − contournement)`). Sans cette dernière règle, le contournement ne changeait rien : la perte russe venait de la décote sur les invendus écoulés, pas des volumes retenus. Calibrage : pétrole et charbon 0,2 + 0,03/mois, plafond 0,35 ; gaz 0,05 + 0,01/mois, plafond 0,15 ; minerais, acier, alimentation 0,2 + 0,03, plafond 0,4. Résultat (graine 42, sans bruit) : **PIB russe −4,5 % à deux ans** (cible −4 à −6 %), prix du gaz à l'import +34 % la première année. Avec un plafond de 0,7 la Russie finissait par gagner (+2 %) grâce aux prix. Sauvegarde : `circumvention` devient un enregistrement par bien → **`schemaVersion: 4`**, v3 figée dans `saveV3.ts`, migration `v3-to-v4.ts` testée sur une vraie sauvegarde J3 (`save/fixtures/j3-europe-10.vsave`, France en guerre contre l'Espagne au 11 avril 2027, dix blindées au front, tuiles contestées, deux sanctions) produite par `tools/veritable/headless/fixture.ts` avant tout changement de schéma. Le J4 poursuit dans la v4.
  3. **Ports et villes à portée comptés dans le ravitaillement sur le cœur** : `CoreBridge` compte les unités `Port` et `City` de la nation à moins de `logistics.range` (40) tuiles (Chebyshev) d'une tuile du segment, avec rejet rapide par boîte englobante. Dette du J3b soldée.
  4. **Offres de paix IA → IA refusées : plus dans le journal** (ni proposée ni refusée) ; seule la signature y figure. Les offres qui impliquent le joueur restent journalisées.
  5. **Stabilité de l'agresseur sanctionné** : malus d'opinion `sanctionsOpinionWeight` (0,2) × part des partenaires commerciaux (distance × PIB, reste du monde compris) qui sanctionnent la nation, sur tous les groupes et sur le proxy IA, tant que les sanctions durent. Résultat, avec le contournement plafonné de la correction 2 qui durcit aussi la dépendance commerciale : France à trois ans 0,478 contre 0,594 pour le témoin (écart 0,115 ≥ 0,08 demandé ; 0,085 avant le plafonnement), PIB français −12 % à trois ans (−9 % au J3) ; critère ajouté au test de livraison 2 et au rapport.
- **Vitesse des fronts : non changée, question ouverte.** Lukas n'a pas vu les avancées en Espagne en temps réel : à `v0` = 0,1 un segment prend ~60 tuiles par mois à rapport 2 (2 tuiles par jour), invisible à l'échelle de la carte. Essai à `v0` = 0,3 / `vMax` = 2 : 25 373 tuiles nettes à trois ans valant 571 Md$ contre 426 Md$ de PIB perdu — la guerre rapporte, le critère du J3 tombe. Deux options soumises : (a) valoriser les tuiles prises au-dessous du PIB moyen par tuile (les marches frontalières ne valent pas le pays entier) et relever `v0` ; (b) garder le rythme et améliorer la lisibilité (surlignage du front, flèches). À trancher avant de toucher `config.json`.
- **Chevauchement des écrans Véritable avec l'interface héritée** : prévu au J7 (« Polissage UI ») ; une correction ponctuelle (z-index, masquage des panneaux hérités inutiles en campagne) peut se faire plus tôt si Lukas nomme les éléments qui gênent.
- **Outil `tools/veritable/headless/fixture.ts`** : écrit une vraie sauvegarde du build courant (cœur, vraie carte, script rejoué) pour les tests de migration de la version suivante ; `Driver.snapshot()` ajouté aux deux pilotes. `reportJ3.ts --only <tests>` ne rejoue que les tests nommés.

## 2026-09-23 — J4 : le moteur (session Claude Code, sans validation intermédiaire)

Plan et écarts : `docs/veritable/plans/J4.md`. Formules : celles de la consigne du 2026-09-23 ; ce qui suit consigne les choix qu'elle laissait ouverts et les calibrations.

### Données

- **Régimes** (`politics/regimes.json`) : les neuf archétypes avec intervalle d'élection, règle de succession, mode de formation du gouvernement (`coalition` | `leading-party` | `appointed`), drapeau `democratic`, `coupBase` et `legitimacyBase` de la consigne, liberté de la presse et contrôle des médias structurels, domaines de lois. **Le drapeau « élections suspendues en cas de guerre sur son territoire » vit dans la fiche nation** (`politics.electionsSuspendedAtWarAtHome`, vrai pour UKR) et non dans le régime : c'est un fait national, pas un trait d'archétype. Intervalle par nation dans la fiche, date de la dernière élection réelle aussi : la prochaine = dernière + intervalle (FRA avril 2027, ITA septembre 2027, ESP juillet 2027, POL octobre 2027, TUR mai 2028, DEU février 2029, GBR juillet 2029, NOR septembre 2029, RUS mars 2030 ; UKR échue en 2024, suspendue par la guerre).
- **Ingestion Wikidata (CC0)** : `tools/veritable/ingest/wikidata.ts`, requêtes SPARQL (P35, P6, P569, P102, P1142, P488) et API de recherche, réponses commitées dans `snapshots/wikidata/<iso3>.json` avec sha256 dans `sources.lock.json` ; pause de 1,5 s entre requêtes et reprise avec recul sur 429 / 5xx (Wikidata limite vite). **Les partis principaux sont listés à la main** dans `parties.json` (au plus huit par nation, QID résolu par libellé exact et pays, part de la dernière élection nationale marquée `estimate` avec le nom de l'élection) : Wikidata ne donne pas de résultats électoraux exploitables sans modéliser chaque scrutin. Certains items n'ont plus de libellé (Macron, Farage) : repli sur le titre Wikipédia. **Les chefs d'État et de gouvernement sont ceux que Wikidata donne au jour de l'extraction** (2026-09-23), pas ceux du 1er janvier 2026 : c'est une donnée datée, rejouable.
- **Table des idéologies** (`politics/ideologies.json`) : 161 QID → trois axes, écrite à la main ; l'idéologie d'un parti = moyenne des idéologies connues ; repli manuel dans `estimates.json → partyIdeologies` pour les partis sans P1142 (BSW, AVS, Sumar, KO).
- **Traits** : chefs d'État et de gouvernement des dix nations écrits à la main et justifiés (`estimates.json → leaderTraits`, clé `<iso3>/<rôle>`, `person` = libellé Wikidata à l'écriture, l'outil signale un changement) ; chefs de parti dérivés de l'idéologie (`traitsFromIdeology`, bruit nul à l'ingestion : agressivité = 0,3 + 0,2 × autorité + 0,2 × souveraineté, corruption = 0,5 × (1 − légitimité de base du régime), charisme et compétence 0,5).
- **Noms** : `names/<iso3>.json` (30 prénoms, 30 noms par nation). Nom fictif d'un dirigeant réel tiré une fois à l'ingestion (hachage FNV de l'identifiant → réservoirs, écrit dans `fr.json`) ; nom parodique écrit à la main dans `fr.json` ; un dirigeant généré en campagne porte un nom littéral tiré au `Rng` (`ActorState.name` = `{kind:"key"}` | `{kind:"literal"}`). `config.leaderNames` choisit la clé affichée.
- **Lois** : 46 lois dans `laws/<domaine>.json` (8 économie, 9 social, 5 sécurité, 13 institutions dont 7 réformes constitutionnelles et une transition civile, 5 environnement, 6 défense). Effets `once` / `while` avec une petite liste de cibles (DATA-SCHEMAS.md). La loi de contrôle des médias : ×1,2 au sortant via `mediaControl` = 0,5, légitimité −0,1, relations avec les démocraties −20.
- **Objectifs** : 14 dans `politics/objectives.json`, conditions évaluées mensuellement sur la vue du monde ; `baseline` capturée à l'épinglage (PIB, élections gagnées) ; les conditions de durée (stabilité, pénurie, paix) comptent les mois consécutifs dans `progress`.

### Règles (choix hors consigne)

- **Dirigeant en jeu** : le chef de l'État dans les régimes présidentiel, semi-présidentiel, autoritaire électoral et nommés ; le chef du gouvernement dans les régimes parlementaires (le monarque règne, le premier ministre gouverne). Gouvernement initial = parti du chef du gouvernement, complété par proximité idéologique jusqu'à la majorité dans les régimes de coalition. Turquie : Wikidata ne donne pas de chef de gouvernement (régime présidentiel) ; le schéma n'exige qu'un chef d'État ou de gouvernement.
- **Projection électorale** : le même calcul que le vote, sans tirage ; exposée dans la vue (`electionProjection`) pour l'écran. Les parts sont normalisées sur les partis en jeu (les petits partis hors données sont ignorés).
- **Leviers** : les leviers du joueur n'ont d'effet qu'à ses élections ; la fraude est remise à zéro après le scrutin ; propagande et clientélisme coûtent chaque mois (propagande = part du PIB, clientélisme = `clientelismCostPctGdp` 0,3 % du PIB) via un transfert négatif dans le budget ; la fuite de corruption multiplie le coût des programmes par (1 + 0,1 × corruption ; 0,2 d'abord, ramené à 0,1). Contrôle des médias structurel d'un régime (autoritaire électoral 0,5) : bonus au sortant de tous ses scrutins, comme une loi.
- **Détection de la fraude** : `p = min(1, 4 × fraude × (1 − contrôle des médias) × liberté de la presse)` (`fraudDetectionScale` = 4 pour que 20 % de fraude avec une presse à 0,9 soient détectés dans ~72 % des cas ; critère > 60 %).
- **Capital** : régénération de la consigne ; +30 à une victoire du gouvernement en place ; un gouvernement neuf part à 30 ; un objectif atteint +20. **Légitimité** : revient vers la base du régime (+ modificateurs des lois) de 0,01 par mois.
- **Curseurs progressifs** : la valeur en vigueur ferme 1/6 de l'écart à la cible chaque mois (`rampMonths` = 6 : 95 % en ~16 mois, exponentiel et sans état supplémentaire) ; la règle budgétaire IA règle aussi les cibles. Écart à la lettre du plan (« rejoint la cible en rampMonths »), consigné.
- **Coups** : la moitié des tentatives échouent (`failureShare` 0,5 : journal, stabilité −0,1, militaires −0,1). Succès : régime `junta`, chef militaire généré, légitimité 0,4, démocraties −30, **suspension par les blocs à `suspendsOnCoup`** (UE) : plus de bonus commercial, d'alignement ni de règle budgétaire ; réintégration dès que le régime est de nouveau démocratique. La suspension vit dans `NationPolitics.suspendedFrom` et le contexte économique en tient une vue synchronisée.
- **Révolution** : gouvernement provisoire dirigé par le premier parti hors du gouvernement renversé (ou une figure générée), régime parlementaire si l'ancien n'était pas démocratique, élections dans six mois, légitimité 0,5, PIB −5 %, stabilité −0,2, groupes remis à 0,5.
- **Nations IA** : chocs mensuels d'opinion `N(0, 0,06 × (1 − L) × (1 + 10 × coupBase))` ; **relations vers une affinité** `40 × blocs communs + 20 × (1 − distance idéologique des gouvernements / distance maximale)`, à +2 par mois en dessous et −0,5 au-dessus ; remplace le retour vers 0 du J3 (deux membres d'un même bloc restent amis). `relationDecayPerMonth` passe de 1 à 0,5 (c'est le ±0,5 de la consigne). Le terme de blocs est plafonné à `blocRelationCap` (60) : sans plafond, quatre blocs communs valaient 160 et les relations franco-allemandes montaient à 100. Même plafonnée, l'affinité fait monter les relations de la France avec ses partenaires de l'UE vers 78 pendant 2026, là où le J3 les érodait vers 48 : une guerre d'agression part de plus haut. **Un agresseur sans casus belli ne recolle rien tant que sa guerre dure** (ses relations ne font que s'éroder) : c'est la lecture d'« érosion mensuelle uniquement pour l'agresseur » ; sans cette clause, deux sanctionneurs seulement à six mois au test de livraison 2 du J3 (critère : cinq).
- **Coalition : l'agresseur est comparé à sa victime, et non plus à l'ensemble des défenseurs** (correction d'une règle du J3 ; la consigne disait « agresseur > 1,5 × la victime »). Le J3 additionnait les coalisés. Dès que la Norvège et l'Ukraine rejoignaient l'Espagne, la France ne pesait plus 1,5 fois « les défenseurs ». L'Allemagne, l'Italie, le Royaume-Uni et la Pologne, pourtant à −100, ne recevaient plus d'appel, et la France gardait 24 000 tuiles espagnoles valant 541 Md$ pour 416 Md$ de PIB perdu : le test de livraison 2 du J3 tombait. Avec la victime seule, ces quatre voisins rejoignent en janvier-février 2028 et reprennent du terrain. Test unitaire dédié dans `diplomacy.test.ts`, qui échoue avec l'ancienne règle.
- **Vieillissement** : probabilité annuelle de la consigne tirée mensuellement (÷ 12) ; à 80 ans 0,7 %/an, à 100 ans 3,8 %/an — faible, mais c'est la formule demandée.
- **Journal des IA → IA** : les élections, gouvernements, coups et révolutions des nations IA sont journalisés (une ligne par événement, une élection tous les quatre à six ans) ; seules les offres de paix refusées restent hors journal (correction du J3).
- **Commandes** : `enact-law`, `repeal-law`, `set-lever`, `pin-objective`, `unpin-objective`, `add-note` ; `set-tax` et `set-spending` règlent les cibles. Pas de commande « liberté de la presse » : elle découle du régime et des lois (écart au plan, consigné).

### Sauvegarde v4

Ouverte par la correction 2 du J3 ; sections politiques ajoutées sans nouvelle version (aucune v4 hors de la machine de développement). Migration `v3-to-v4.ts` : politique construite depuis les fiches et les dirigeants comme une nouvelle campagne, opinion, stabilité, troubles et règle de bloc du J3 conservés ; cibles des curseurs = valeurs en vigueur ; testée sur `save/fixtures/j3-europe-10.vsave`. Les idéologies et traits ont un schéma « fil » propre (`zb.float()`) dans `save.ts`, zbin refusant `z.number()`.

### Résultats de clôture

- **Test de livraison (`docs/veritable/reports/J4/`, simulation seule, 10 ans × 10 graines 42-51, ≈ 0,6 s par campagne)** : les dix nations connaissent au moins une alternance dans au moins une graine (critère : sept), 9 ou 10 nations par campagne ; 200 élections, 127 alternances ; crises dans les dix campagnes (31 coups réussis, 25 tentatives, 13 débuts de troubles, 0 révolution) ; aucune junte sans cause (toute junte finale suit un coup de la campagne). Test de fraude : 1 000 élections à 20 % de fraude avec une presse à 0,9 → détection dans 72 % des cas (critère > 60 %).
- **Observation à trancher — fréquence des coups.** Avec la formule de la consigne et ses constantes, un régime autoritaire électoral (Russie, Turquie : `coupBase` 0,01, légitimité 0,5) a au moins 0,9 % de probabilité de coup par mois quelle que soit la satisfaction des militaires (le facteur `1 + 3 × (1 − s)` vaut 2,5 à s = 0,5, jamais moins de 1), soit ≈ 2 % par mois observés et une junte en Russie dans les dix graines (14 coups sur dix campagnes), en Turquie dans six. Les démocraties (`coupBase` 0,001-0,002, légitimité 0,8) tombent une fois sur dix à quinze par décennie (France 3, Italie 2, Ukraine 4, Pologne 1, Norvège 1 sur dix graines). Deux lectures possibles : garder la formule (elle est celle de la consigne) ou compter la **mécontentement** des militaires plutôt que leur satisfaction (`1 + 3 × max(0, 0,5 − s) / 0,5`, facteur 1 au neutre), ce qui divise toutes les probabilités par 2,5. Non tranché : la formule de la consigne est en place. **Période de grâce depuis le début de la campagne** : le régime de départ est daté du 1er janvier 2026, si bien qu'aucun coup n'est possible avant janvier 2029, ni pour les IA ni pour le joueur. Elle évite une vague de coups en 2026 avec la formule actuelle, mais elle neutralise le risque de coup doublé d'une fraude détectée à la présidentielle française d'avril 2027 (il expire en avril 2028). À trancher avec la fréquence des coups : ne compter la grâce qu'après un changement de régime survenu en campagne (`regimeBefore` non nul) est une ligne de code, mais ramène la question de la formule dès 2026.
- **Cinquante ans headless (test `campaign.test.ts` du J2)** : l'Italie, tombée en junte à la graine 42 (légitimité 0,4, suspendue par l'UE, taux d'intérêt relevé par l'instabilité), fait défaut en 2068. Le test tolère désormais un défaut dans une nation qui a connu un coup ou une révolution, et exige toujours qu'aucune nation stable ne fasse défaut ni ne voie sa dette diverger. Halver la fuite de corruption (0,2 → 0,1) ne retardait le défaut italien que de deux ans : c'est la junte, pas la corruption. À la graine 43, sans coup, la dette italienne divergeait quand même (1,99 à cinquante ans) : la règle budgétaire IA réglait des cibles que la rampe de six mois suivait avec retard, boucle trop molle ; **la règle IA applique désormais ses cibles immédiatement** (les nations IA n'ont pas de rampe), comme au J3.
- **Aucune révolution** sur mille années-nations (10 nations × 10 ans × 10 graines) : trois groupes sous 0,25 et une pénurie > 0,3 ou une stabilité < 0,3 pendant six mois ne se rencontrent pas dans une économie de temps de paix ; le mécanisme est testé unitairement ; les événements du J5 devraient le déclencher.
- **20 ans × 10 graines sur le cœur OpenFront (graines 42-51)** : prix entre 0,94 et 1,07 × base, dette/PIB maximale 1,49 (Italie, comme au J3), aucun défaut, aucun trouble, 53 coups sur deux mille années-nations, une quarantaine d'élections par campagne ; la guerre russo-ukrainienne se termine par une paix dans chacune des dix campagnes. Une première série, lancée avant que la règle budgétaire IA n'applique ses cibles immédiatement, montait à 1,65 de dette.
- **Tests de livraison du J3 rejoués** (`docs/veritable/reports/J3/`, graine 42, sans bruit) : embargo UE, PIB russe −4,5 % à deux ans et gaz à l'import +34 % la première année ; France → Espagne, 6 sanctionneurs à six mois, coalition de huit nations, 5 524 tuiles nettes valant 124 Md$ pour 412 Md$ de PIB perdu, stabilité 0,496 contre 0,607 pour le témoin ; blocus britannique, commerce maritime norvégien −47 % ; débarquement italien refusé.
- **Wikidata en l'état au 2026-09-23** : chef du gouvernement britannique Andy Burnham, roi de Norvège Haakon VIII, premier ministre ukrainien Serhiï Koretskyï, chef des libéraux-démocrates Nick Clegg, chef de la Lega Giulio Centemero : ce sont les valeurs de Wikidata à l'extraction, conservées telles quelles (données datées, rejouables) ; les traits manuels des chefs ont été écrits pour ces personnes. Un premier passage lisait l'appartenance partisane (P102) sans qualificatif de fin : Macron ressortait au Parti socialiste ; la requête ne lit plus que les appartenances sans date de fin.
- **`npm test`** : 6 847 tests passent, seuls les trois échecs connus hors jeu restent (`UpdateRegister` × 2, `ClanDonateDialog` × 1) ; lint et `build-prod` propres (`static/` supprimé ensuite).
- **Écrans** vérifiés dans le navigateur (dev, France) : Politique (loi votée, capital 30 → 10, cible du poste défense +1 point), Élection (leviers, projection), Dirigeants, Objectifs et journal (objectif épinglé, note journalisée). Le chevauchement avec le classement et le tutoriel hérités reste (J7).

## 2026-09-24 — J5a : corrections du J4, fronts, interface héritée, nucléaire, IA des nations (session Claude Code, sans validation intermédiaire)

Plan et écarts : `docs/veritable/plans/J5.md`.

### Décisions du J4 validées par Lukas (2026-09-23)

- **Relations** : retour vers l'affinité à +2 par mois par en dessous, −0,5 par au-dessus (`relationRecoveryPerMonth`, `relationDecayPerMonth`).
- **Détection de la fraude** : facteur 4 (`fraudDetectionScale`).
- **Règle budgétaire IA sans rampe** : elle applique ses cibles aussitôt.
- **Tolérance du test de cinquante ans** : un défaut n'est admis que dans une nation qui a connu un coup ou une révolution.
- **Licence V-Dem** : à vérifier au J6, avant toute ingestion.

### Corrections du J4

- **Dirigeants au 1er janvier 2026** (date de départ du scénario, `referenceDate` des instantanés) et non plus au jour de l'extraction. Chaque mandat (P6, P35, P488) et chaque appartenance partisane (P102) est lu avec son rang et ses qualificatifs P580 / P582 ; le filtrage se fait dans l'outil, les mêmes filtres écrits en SPARQL dépassant le délai du point d'accès public. Parmi les déclarations valides à la date : le rang préféré d'abord, puis la plus récente. **Un président de parti mort avant la date est écarté** (P570) : Wikidata gardait Silvio Berlusconi à la tête de Forza Italia faute de date de fin ; c'est Antonio Tajani. Les fiches des dirigeants et les données politiques des fiches nations sont datées du 1er janvier 2026 ; aucune donnée du scénario n'est postérieure. Ingestion rejouable : `npm run veritable:ingest -- fetch-wikidata --scenario europe-10` puis `build-leaders`.
- **Chefs d'État et de gouvernement au 1er janvier 2026, conformes à la liste attendue** : France Emmanuel Macron et Sébastien Lecornu ; Allemagne Frank-Walter Steinmeier et Friedrich Merz ; Royaume-Uni Charles III et Keir Starmer ; Italie Sergio Mattarella et Giorgia Meloni ; Espagne Felipe VI et Pedro Sánchez ; Pologne Karol Nawrocki et Donald Tusk ; Ukraine Volodymyr Zelensky et Ioulia Svyrydenko ; Russie Vladimir Poutine et Mikhaïl Michoustine ; Turquie Recep Tayyip Erdoğan ; Norvège Harald V et Jonas Gahr Støre. Traits réécrits à la main pour les trois titulaires nouveaux par rapport au J4 (Keir Starmer, Ioulia Svyrydenko, Harald V) ; onze noms parodiques ajoutés ; 28 clés de personnes qui ne figurent plus dans les données retirées de `fr.json`. **Chefs de parti signalés, non tranchés** (Wikidata donne souvent le président du parti plutôt que son dirigeant) : PSOE Cristina Narbona (Pedro Sánchez en est le secrétaire général), Parti démocrate Stefano Bonaccini (Elly Schlein secrétaire), Lega Giulio Centemero, Solidarité européenne Vitali Klitschko (Petro Porochenko), Russie juste Zakhar Prilepine (Sergueï Mironov), Coalition civique Barbara Nowacka. Une surcharge manuelle par parti dans `parties.json` corrigerait ces cas ; décision à prendre par Lukas.
- **Outil d'ingestion plus robuste** : nouvelle tentative sur une connexion coupée par le serveur (une coupure a interrompu l'extraction pendant la Norvège), verrou écrit après chaque nation, `--only` pour ne refaire que certaines nations, `relock-wikidata` pour les empreintes d'instantanés écrits par une course interrompue.
- **Coups, formule du J5** : p = coupBase × 4 × (1 − satisfaction des militaires)² × (1 + 2 × (1 − stabilité)) × (1 − légitimité) × (1 + épuisement) × 2 dans l'année d'une fraude détectée × lois en vigueur (`militaryScale`, `militaryExponent`, `stabilityWeight`). coupBase : parlementaire 0,0001 ; présidentiel 0,0003 ; semi-présidentiel 0,0002 ; autoritaire électoral 0,001 ; parti unique, monarchie absolue et théocratie 0,0005 ; junte 0,005 ; État failli 0,02. **Plus de période de grâce au début de la campagne** ; une consolidation de douze mois suit tout changement de régime survenu en campagne (`consolidationMonths`, `regimeBefore` non nul). La junte rend toujours le pouvoir après quatre ans (5 % par mois). **Comptage corrigé** : un coup réussi n'émet que « coup-succeeded », une tentative déjouée que « coup-attempted » ; le rapport compte désormais les tentatives (réussies et déjouées) et les déjouées à part (31 réussis pour 25 tentatives était une lecture fausse).
- **Résultat** (rapport du J4 rejoué par `reportJ4.ts`, 10 ans × 10 graines 42-51, simulation seule) : 3 coups réussis pour 4 tentatives ; junte en Russie dans une graine, en Turquie dans une autre (cible : deux au plus chacune) ; aucun coup dans une démocratie restée au-dessus de 0,5 de stabilité ; alternances dans les dix nations ; aucune junte sans cause. **Crise politique dans 3 campagnes sur 10 seulement** : au J4 les coups faisaient presque toutes les crises ; aucun trouble ni aucune révolution en dix ans de paix. Le critère de livraison du J5 (« crise dans la moitié des campagnes à dix ans ») repose donc sur les événements du J5c (crise alimentaire, choc énergétique, émeutes contre l'austérité).
- **Mortalité** : probabilité annuelle 0,001 × e^(0,085 × (âge − 30)), environ 7 % à 80 ans (`leaders.deathBase` 0,0001 → 0,001).
- **Défaut hérité du J3 corrigé** (`ai/war.ts`) : sans front calculé, l'IA de guerre sortait avant la règle de paix ; dans le runner sans cœur, une guerre ne finissait jamais (la guerre russo-ukrainienne durait cinquante ans et la dette ukrainienne dérivait).
- **Dons extérieurs de temps de guerre** (Ukraine, 26 % du PIB, estimation du J2) : ils décroissent de 5 % par mois en paix (`budget.grantsPeaceDecayPerMonth`). Sans cela l'Ukraine accumulait, une fois la paix signée, des excédents énormes (dette à −7 fois le PIB en cinquante ans) ; l'aide de guerre passe désormais par les livraisons d'armes de l'IA.
- **Tests** : le test de cinquante ans sans cœur mesure la couverture en gaz de l'Allemagne en moyenne sur 2029 (un seul jour dépendait du choc d'offre tiré) ; la dette d'une nation stable doit finir sous le maximum de sa valeur de départ et du seuil prudent, plus 0,25, et, au-dessus du seuil prudent, ne plus monter sur les dix dernières années (plus de 0,05 au-dessus des dix précédentes).

### Fronts sur la carte et contestation des tuiles

- **Registre de contestation par tuile** (`sim/war/contest.ts`) : un `u16` par tuile, 0 = non contestée, sinon mois de la dernière prise + 1, bit 15 = cédée par traité. Chaque monde (cœur, mémoire, frontières) en tient un ; la simulation lui donne le mois courant et le règle une fois par mois. Une contestation prend fin cinq ans après une cession par traité (`war.contest.cessionMonths` = 60), dix ans après la dernière prise sans traité (`warMonths` = 120), ou quand plus personne ne tient la tuile.
- **Valeur d'une tuile contestée : 50 %** (`war.contest.valueShare`) dans le score de guerre, le coût d'une cession, le gain espéré par l'IA et l'évaluation du test 2 du J3 (colonne `contested_<nation>` des séries headless, `gainedTerritoryValue`). Une tuile ne produit toujours pas de PIB par elle-même.
- **Cession** : un traité fait passer au délai court toutes les tuiles contestées que tient le vainqueur ; une annexion compte comme un traité.
- **vMax = 3 tuiles par tick**, v0 inchangé (0,1).
- **Décomposition des facteurs d'un segment** dans la vue (`SegmentSide`) : divisions engagées, effectif, équipement et entraînement moyens, ravitaillement, air, terrain et structures (en défense), force ; attaquant et rapport de force effectif (`attacker`, `attackRatio`).
- **Couche carte** (`ui/FrontOverlay.ts`, campagne seulement, entrée `// VERITABLE:` dans `GameRenderer`) : canevas 2D transparent au-dessus de la carte WebGL, redessiné quand la caméra ou les données changent ; données du worker une fois par seconde (requête `map-overlay` : points des segments, centres de tronçons de 12 tuiles, `war.overlayStep`, et tuiles contestées renvoyées seulement quand elles ont changé). Couleurs vues du joueur : vert il avance, rouge l'ennemi avance, jaune et orange attaque contenue ; entre deux autres nations, bleu ou violet selon le camp qui avance ; front calme en tirets clairs ; épaisseur selon la marge. Étiquette « FRA ×2,4 » au milieu de chaque segment attaqué, au-delà d'un certain zoom. Tuiles contestées hachurées. Clic sur un segment : panneau des facteurs des deux camps. Légende en bas à gauche.
- **Sauvegarde v5 ouverte** : `saveV4.ts` figé ; le conteneur `.vsave` porte depuis la v5 un second bloc RLE, la contestation de chaque tuile ; `territory` (tuiles de chaque nation au premier jour). Migration `v4-to-v5.ts` : une tuile marquée contestée en v4 l'est depuis le mois de la sauvegarde, non cédée ; territoire du premier jour lu dans les frontières du scénario quand l'appelant les a (`MigrationContext.initialTiles`), sinon dans la sauvegarde. Testée sur la vraie sauvegarde J4 (`save/fixtures/j4-europe-10.vsave`).
- **Test de livraison 2 du J3 rejoué** (cœur, graine 42, vMax 3, tuiles contestées à 50 %) : 7 sanctionneurs à six mois, coalition de huit nations, 16 569 tuiles nettes à trois ans (46 159 tuiles contestées tenues par la France) valant 186 Md$ pour 430 Md$ de PIB perdu, stabilité 0,486 contre 0,609 pour le témoin : la guerre sans casus belli ne paie toujours pas.

### Interface héritée en campagne

- **Masqués par une feuille de style par balise** (plusieurs composants gèrent eux-mêmes leur visibilité) : classement, panneau de contrôle (troupes, or, ratio), surimpression au survol, panneau joueur et dons, fils d'événements hérités, attaques en cours, chat et émojis, vitesses de relecture, tutoriel hérité, minuteurs de spawn et d'immunité, message d'accueil, fenêtre de victoire, cadre d'alerte, encart promotionnel. **Non exécutés** : menu radial, fenêtre de victoire, tutoriel. **Barre de droite** : minuteur, pause et avance rapide masqués ; réglages, plein écran et sortie restent. Aucun encart publicitaire en coin. Raccourcis d'attaque au sol, de bateau, de riposte et d'alliance bloqués comme le clic. Inventaire dans le plan.
- **Usine, bombes A et H, MIRV désactivées** (`disabledUnits`) ; **or hérité illimité** (`infiniteGold`) : ce que la nation construit sur la carte est payé par son budget national le mois suivant (`budget.structureCostUsd` : ville 5 Md$, port 2, silo 3, SAM 1, navire de guerre 1,5, poste de défense 0,5 ; niveaux comptés ; estimations), ligne « Constructions » de l'écran Budget, comptes dans la sauvegarde v5 (`territory.structures`). Une structure perdue puis reconstruite est payée de nouveau.
- La vidéo publicitaire qui s'affiche parfois en bas à gauche dans le panneau de l'application n'est pas dans le DOM de la page (réseau publicitaire d'OpenFront) : sans objet pour la version Electron du J7.

### Nucléaire

- **Niveau de menace** quotidien d'une nation nucléaire : 0 en paix ; 1 en guerre ; 2 si un ennemi lui a pris des tuiles dans une guerre en cours ou si sa capitale est à moins de `capitalFrontTiles` = 100 tuiles d'un front ; 3 si elle a perdu plus de la moitié de ses tuiles du premier jour, sa capitale, ou si sa stabilité est sous 0,15 (ou si elle est en exil).
- **Probabilité journalière de tir** = `base[doctrine][niveau]` × (0,5 + agressivité du dirigeant) × dissuasion (0,1 si la cible a des ogives ou appartient à un bloc de défense collective, l'OTAN, qui compte un membre nucléaire). Table : emploi en premier possible et non déclarée [0 ; 1e-6 ; 1e-4 ; 2e-3], non-usage en premier [0 ; 0 ; 2e-5 ; 1e-3], imprévisible [0 ; 5e-6 ; 3e-4 ; 4e-3] ; à calibrer sur la campagne de livraison. Seules les nations IA tirent d'elles-mêmes ; le joueur tire par commande, avec double confirmation à l'écran.
- **Cibles** : premier tir (bombe A) sur le segment de front où la cible engage le plus de divisions ; ensuite bombes H sur la capitale puis les villes, les plus grandes d'abord.
- **Vecteurs** : un silo OpenFront près de la capitale de chaque nation nucléaire dès le premier jour ; pour une sauvegarde antérieure, un silo est posé au premier tir. Le tir passe par `NukeExecution` ; l'interception SAM du cœur s'applique. **Retouche du cœur** (`// VERITABLE:`, `PlayerImpl`) : en campagne, `canBuildUnitType` et `buildUnit` laissent passer les bombes A et H désactivées ; `ConstructionExecution` et la barre d'unités continuent de les refuser au joueur.
- **Issue d'un tir** : le monde note au lancement le propriétaire de chaque tuile du rayon d'effet ; une fois l'ogive disparue, les tuiles passées en retombées donnent les tuiles touchées par nation (aucune : interceptée ; ogive jamais construite : échec, rendue à l'arsenal).
- **Conséquences** : relations de tous avec le tireur à −80 au plus ; sanctions de toutes les nations IA (la nation du joueur décide elle-même) ; le tireur devient paria : une coalition peut se former contre lui quels que soient sa puissance et son camp (les appels de coalition portent désormais le camp à rejoindre). **Retombées** : PIB, production, consommation et effectifs mobilisables de la nation touchée × (1 − 0,9 × part de ses tuiles touchées), sans reconstruction avant le J7.
- **Main morte** : à l'annexion d'une nation nucléaire, p = facteur[doctrine] (0,5 ; 0,3 ; 0,5 ; 0,8) × (0,5 + agressivité), plafonnée à 1, qu'elle frappe la capitale de l'annexeur ; l'annexion est alors reportée au lendemain (le silo changerait de camp avant le départ de l'ogive). Probabilité affichée dans le formulaire de paix (annexion) et dans le panneau nucléaire.
- **Ogives en vol** non sauvegardées (règle du J0) : au rechargement elles sont « perdues » (statut `lost`).
- **Sauvegarde v5** : `nuclear` (arsenaux, frappes, retombées), `diplomacy.pariahs`, `diplomacy.pendingAnnexations`, camp des appels de coalition ; la migration v4 → v5 part des arsenaux des fiches.
- **Écran** : section Nucléaire de l'écran Fronts et divisions (arsenal, doctrine, niveau de menace, risque annuel de tir de chaque puissance, main morte, tirs récents, boutons de tir à double clic). Vérifié dans le navigateur : tir français sur Varsovie, 26 034 tuiles ravagées, livraisons d'armes allemandes, italiennes et espagnoles à la Pologne, entrée de la Russie en coalition contre la France.

### IA des nations

- **Agenda** dans les fiches (`aiAgenda`, quatre poids de somme 1), écrit à la main et justifié dans `estimates.json`, recopié par `veritable:ingest build`.
- **Cycle échelonné** : à chaque tick du cœur, un dixième des nations passent en revue (curseur tournant) ; une nation décide si sa date de revue est passée : un mois sans enjeu, une semaine en guerre, en crise (troubles ou stabilité sous 0,35) ou en litige avec le joueur (sanctions). Dates et curseur sauvegardés (`ai`, v5).
- **Défense selon la menace** : objectif = part du premier jour × (1 + hausse × (0,5 + poids sécurité)), hausse 1 en guerre, 0,5 face à un voisin terrestre plus fort à moins de −40 ; plafond 6 % du PIB ; l'IA ne fait que relever la défense. La règle budgétaire suit cet objectif en paix (au lieu du niveau du premier jour) et n'ampute jamais la défense en guerre.
- **Déclaration de guerre** : voisin terrestre, casus belli réel ou agressivité du dirigeant > 0,8, puissance au moins double, jamais contre une nation dotée sans l'être soi-même ; gain espéré sur dix ans (part des terres prises, 3 % par point de rapport de force au-delà de 1, plafond 15 %, × PIB de la cible × 50 % × poids influence et sécurité, par an) supérieur au coût espéré sur trois ans de guerre (sanctions attendues des partenaires qui partagent un bloc avec la cible, épuisement 2 % du PIB, réputation 1 % ou 3 % sans casus belli, × poids croissance, par an) : les terres restent, les coûts cessent avec la paix ; et même alors un tirage (5 % × (0,5 + agressivité) par revue), au plus une guerre tous les cinq ans. **Mesure provisoire** (monde sans cœur, 10 graines × 50 ans) : une seule guerre nouvelle en dix campagnes. Ce qui bloque n'est pas le calcul mais l'absence de casus belli : après la guerre du scénario, aucune paire n'en a, et un dirigeant à plus de 0,8 d'agressivité est rare (le successeur de Vladimir Poutine est à 0,24). La règle de la consigne reste telle quelle ; la fréquence visée (médiane de 1 à 4 guerres par campagne) se calibre au J5c avec les événements, qui peuvent ouvrir des casus belli.
- **Livraisons d'armes** : chaque mois, une nation IA en paix envoie 10 % de son armement du mois, pris d'abord sur ses exportations, au belligérant qu'elle préfère (relations > 40) s'il combat un ennemi qu'elle déteste (< −40) ; les divisions du receveur se rééquipent d'autant ; le donneur paie au prix de base de l'armement. Journal au début et à la fin d'une livraison.
- **Marine** : blocus de l'ennemi côtier combattu, seulement si sa flotte est plus faible ; débarquement si le contrôle de la zone entre belligérants atteint 60 %, là où aucun front terrestre ne touche l'ennemi, au plus un tous les six mois.

## 2026-09-24 — J5b : blocs, couches 2 et 3 (session Claude Code, sans validation intermédiaire)

Plan et écarts : `docs/veritable/plans/J5.md`.

### Données

- **Douze blocs du palier 1** (`data/veritable/blocs/*.json`) avec **les listes de membres de tous les pays** (ISO 3166-1 alpha-3, statuts membre, candidat, observateur, partenaire, associé, suspendu), écrites à la main d'après les listes officielles de 2025 ; règles de décision par domaine (sanctions, levées, adhésions, suspensions, budget, défense, technologie, commerce), majorité qualifiée, budget, adhésion, sortie, leadership, défense collective, compétences, branche technologique (au J5c). Schéma `data/schemas/bloc.ts` réécrit ; `decisionRules`, `accession`, `exit` et `leadership` sont obligatoires. **Écart de DATA-SCHEMAS.md**, section mise à jour : `joinCriteria` devient `accession` (avec la durée du processus), `exit.cost` devient `exit.tradeCostPctGdp`, `leadership` est un objet (genre, durée du mandat, ordre), `budget.spending` devient `budget.shares` (structurels, défense, candidats, programmes) ; ajout de `qualifiedMajority` et `collectiveDefense` ; le critère géographique attend le J6.
- **UE** : majorité qualifiée 55 % des votants et 65 % de leur population ; unanimité pour sanctions, levées, adhésions, suspensions et défense ; majorité qualifiée pour budget, technologie et commerce ; contribution 1 % du PIB (fonds structurels 50 %, défense 15 %, candidats 5 %, programmes 30 %) ; adhésion : régime démocratique, dette sous 120 % du PIB, relations moyennes ≥ 20, 24 à 60 mois ; sortie : 24 mois, 2 % du PIB ; présidence tournante de six mois dans l'ordre officiel à partir de Chypre (1er semestre 2026). Compétences « défense » (PSDC, Fonds européen de la défense) et « technologie » (programme-cadre de recherche) ajoutées à celles du J3.
- **OTAN** : consensus, défense collective (article 5 : 0,9, ou 0,5 si le souverainisme du gouvernement dépasse 0,7), puissance dominante, budget commun 0,05 % du PIB entièrement en fonds de défense.
- **Artefact europe-10, à lever au J6** : seuls les membres simulés votent, paient et reçoivent ; la présidence tournante de l'UE parcourt l'ordre officiel réduit aux membres simulés (Italie 1er semestre 2026, France 2e semestre, Pologne, Espagne, Allemagne), la puissance dominante de l'OTAN est la plus grande puissance militaire simulée (la Turquie dans nos campagnes, pas les États-Unis), les fonds structurels se partagent entre cinq membres (la Pologne reçoit environ 3 Md$ nets par mois, 4 % de son PIB, contre 1 % dans la réalité).

### Règles

- **Adhésion dynamique** : l'état des blocs (`blocs`, sauvegarde v5) porte les membres ; le contexte économique en tient une vue (`memberships`), qui remplace partout les étiquettes de bloc des fiches : blocs communs des relations et du casus belli « allié attaqué », bonus commercial, règle budgétaire, dissuasion nucléaire, sanctions attendues par l'IA, objectifs. Une étiquette de fiche ne compte plus que pour un bloc sans entité.
- **Vote** : chaque membre simulé non suspendu vote, sauf la nation visée par la mesure. `U = 1 × relations/100 × signe + 0,5 × alignement des gouvernements × signe − 0,5 × commerce en jeu (% du PIB) − 0,5 × souverainisme × intégration + 0,2` (`config.blocs.vote`), signe −1 pour une mesure hostile (sanctions, suspension). Le commerce en jeu est **marginal** : part du partenaire dans les échanges du votant × ouverture × friction des sanctions × enjeu de la mesure (sanctions 1, levée −1, accord commercial −0,5, adhésion −0,25) ; il est nul si le votant sanctionne déjà la cible ou la combat (sinon la victime elle-même votait contre les sanctions de son agresseur). Les mesures du bloc lui-même (budget, défense commune, programme) n'ont pas de cible : ni relations ni alignement, seulement loyauté, coût (budget : variation de la contribution nette) et souverainisme. Première version : l'alignement avec le gouvernement qui propose, écarté parce qu'il faisait tout passer, hausse et baisse du budget comprises. Oui si U > 0 ; le proposant vote oui ; le joueur vote lui-même, ou comme son gouvernement penche tant qu'il n'a pas voté.
- **Règles** : unanimité et consensus = aucun vote contre et au moins un pour (l'abstention ne bloque pas) ; majorité simple = plus de pour que de contre ; majorité qualifiée = part des votants et part de leur population.
- **Calendrier d'un vote** : il se tient le 1er du mois suivant quand le joueur vote (il a le mois pour voter) ; entre nations IA seules, il se tient aussitôt. Sans cela, le vote retardait d'un mois les sanctions que l'alignement de la couche 1 appliquait aussitôt.
- **Leader** : présidence tournante (ordre des données réduit aux membres simulés, nouveaux membres à la fin, mandats comptés depuis le 1er janvier 2026), puissance dominante (plus grande puissance militaire simulée ; elle garde la tête tant qu'elle reste à 10 % du premier, pour éviter un va-et-vient mensuel), direction élue (plus grande économie, aucun bloc du palier 1). Un changement de tête est journalisé.
- **Mesures** (au plus une par mois et par bloc, proposées par la tête) : sanctions (compétence « sanctions ») ; levée de sanctions ou d'une suspension ; adhésion (ouvre le processus d'un candidat ou d'une candidature, si les critères sont remplis) ; suspension d'un membre ; budget ±20 % (échelle bornée entre 0,4 et 2) ; défense commune (compétence « défense », bloc sans clause) ; programme technologique commun de 36 mois (bloc dont le budget a une part programmes) ; accord commercial avec un non-membre (bloc à bonus commercial ; bonus 1,2 entre le partenaire et chaque membre). Capital politique du joueur : sanctions 20, levée 10, adhésion 20, suspension 30, budget 15, défense commune 30, programme 20, accord commercial 15. L'écran montre, pour chaque mesure possible, le vote projeté de chaque membre avec sa valeur de U.
- **Sanctions d'un bloc** : chaque membre simulé sanctionne la cible, le joueur compris (une décision à la majorité le lie) ; un nouveau membre reprend les sanctions en vigueur ; un membre ne les lève pas seul (ni l'IA à sa revue mensuelle ni le joueur par la commande de sanctions) : seul un vote de levée le fait. **L'alignement de la couche 1 (J3) est remplacé par ce vote** : la tête IA propose des sanctions contre un agresseur qu'elle déteste (relations < −40) ou que la moitié des votants sanctionnent déjà.
- **Tête IA** : dans l'ordre, sanctions, levée (plus agresseur, relations ≥ −40), défense commune (un membre attaqué par un non-membre), adhésion d'un candidat, suspension d'un membre agresseur d'un autre membre, puis au hasard un programme (5 % par mois) ou un accord commercial (2 %, partenaire à ≥ 40), ces deux probabilités × (0,5 + poids « croissance » de son agenda) ; seulement une mesure qu'elle veut (U > 0) et que le vote projeté adopte. Aucune mesure de budget, aucune sortie de l'IA au J5b.
- **Budget** : contributions mensuelles = part du PIB × échelle ; fonds structurels vers les membres sous 90 % du PIB par habitant du bloc, au prorata de ce qui leur manque ; fonds de défense vers les membres qui se défendent dans une guerre, sinon au prorata de leur dépense de défense ; aide aux candidats au prorata de leur population ; programmes au prorata de la recherche des membres quand un programme est actif ; ce qui ne trouve pas de destinataire revient aux payeurs. Le solde net passe dans le budget national du mois suivant (ligne « Blocs » de l'écran Budget).
- **Adhésion** : candidature (commande `bloc-apply` ; l'IA postule avec 1 % de chances par mois si elle remplit les critères, que ses relations avec les membres dépassent le minimum de 20 et que son gouvernement n'est pas souverainiste, < −0,3) ; ouverture votée → statut candidat, durée tirée entre le minimum et le maximum ; vote annuel ; vote final à l'échéance → membre, avec les sanctions du bloc. Des critères qui tombent gèlent le processus, un vote négatif aussi. Un bloc sans critères d'adhésion (G7, G20, OPEP) est sur invitation : pas de candidature. Critère géographique non modélisé (toutes les nations de la carte Europe seraient éligibles à l'UE) : au J6, avec les régions.
- **Sortie** : commande `bloc-leave`, effective après le délai ; le PIB perd la part de la sortie (UE 2 %) ; bonus commercial, règle budgétaire, votes et budget cessent avec l'adhésion ; la perte de la branche technologique suivra l'arbre du J5c (l'accès à une branche sera lu sur l'adhésion).
- **Défense collective** : une guerre déclarée en campagne contre un membre par un non-membre ; chaque autre membre simulé IA entre en guerre avec la probabilité de la clause (OTAN 0,9 ou 0,5 ; défense commune votée 0,7 ou 0,4) ; le joueur est appelé (commande `bloc-honor`) et a un mois pour répondre, sinon ses relations avec chaque autre membre baissent de 20. Traitée au pas mensuel des blocs, donc dans le mois. Les guerres du scénario et celles d'une sauvegarde migrée ne déclenchent rien.
- **Sauvegarde v5** : section `blocs` (membres, échelle du budget, sanctions, accords, défense commune, programmes, processus d'adhésion, candidatures, sorties, dernier mois de proposition, contributions et versements du dernier mois ; propositions en cours et résolues sur 24 mois ; appels de défense collective ; guerres traitées ; têtes ; soldes nets). La migration v4 → v5 part des membres des données, calcule la tête du mois et marque les guerres de la sauvegarde comme traitées. **Une sauvegarde v5 écrite par le J5a ne se charge plus** (le schéma v5 n'est complet qu'à la fin du J5, comme le prévoit le plan).

### Écran Blocs

Tableau des douze blocs (tête, membres simulés et monde, statut du joueur, votes en cours) ; pour le bloc choisi : tête et fin du mandat, membres simulés, règles, majorité qualifiée, budget (contribution, versé et reçu le mois dernier), défense collective, sanctions, accords, programmes, processus d'adhésion, candidatures, sorties, appels de défense collective (bouton « Honorer »), votes en cours avec la projection et les boutons de vote du joueur, mesures possibles quand le joueur dirige (coût, vote projeté de chaque membre avec U), critères d'adhésion et candidature, sortie à double clic, décisions récentes. Vérifié dans le navigateur (France, carte Europe) : G7 présidé par la France en 2026, sanctions contre la Russie soumises (capital 30 → 10), une mesure par mois, vote rejeté le 1er février et inscrit à l'historique ; la présidence italienne de l'UE propose un accord commercial avec le Royaume-Uni, la France dispose des boutons de vote pendant le mois, l'accord est adopté le 1er mars et figure dans les accords en vigueur.

### Observations

- **Test 2 du J3 rejoué** (cœur, graine 42, France → Espagne sans casus belli) : 4 nations sanctionnent la France à six mois (6 au rejeu du J4), 8 à sept mois (7 au J4) ; perte de PIB français 437 Md$ contre 154 Md$ de tuiles prises (contestées comptées à 50 %) ; stabilité 0,479 contre 0,607 pour le témoin ; coalition de huit nations. La guerre coûte toujours plus qu'elle ne rapporte, mais le critère « cinq sanctions en six mois » est manqué d'un mois. Cause : le vote pèse le commerce en jeu ; l'Allemagne et l'Italie ne votent les sanctions de l'UE qu'une fois leurs relations avec la France effondrées, sanctionner leur premier partenaire leur coûtant environ 3 % de PIB dans le modèle, alors que l'alignement de la couche 1 ignorait ce coût. **Deux options, à trancher par Lukas** : (a) garder la formule de la consigne telle quelle ; (b) ajouter à l'utilité d'une sanction contre l'agresseur d'un membre un terme de solidarité (relations avec la victime), qui rétablit un vote rapide quand un membre est attaqué. Le rejeu complet des tests du J3 et du J4 a lieu au J5c.
- **Ukraine** : candidate à l'UE dès le premier jour, mais ses relations moyennes avec les membres (≈ 0, faute de bloc commun) sont sous le minimum de 20 : aucune présidence ne propose l'ouverture de son processus dans nos campagnes. Les relations du modèle ne comptent pas le soutien à une victime ; les relations et sanctions initiales du monde de 2026 sont une donnée du J6.
- **Temps de calcul** : le pas mensuel des blocs coûte ≈ 40 ms sur dix ans de campagne sans cœur (0,3 ms par mois).
- **Tests headless du J2 rendus robustes au chemin aléatoire** : les tirages des têtes IA (programmes, accords) décalent le flux du `Rng`, donc les chocs d'offre du monde. La couverture en gaz de l'Allemagne après la coupure russe se mesure désormais en moyenne sur 2029 contre le témoin (le témoin lui-même tombait à 0,89 en janvier 2029 sous un choc mondial) ; la vérification « une dette au-dessus du seuil prudent ne monte plus » ne vaut que pour une dette déjà au-dessus du seuil dix ans avant la fin (la Russie le franchissait dans les derniers mois, 0,605, avant que la règle budgétaire, qui agit après un an de hausse, ait pu jouer).
- **Retouche client corrigée** (`ControlPanel.ts`, `// VERITABLE:` du J3) : l'accès à la configuration tolère les doublures des tests OpenFront ; il produisait 11 rejets non gérés à chaque `npm test` sans faire échouer de test.

## 2026-09-24 — J5c : technologie, événements, livraison du J5 (session Claude Code, sans validation intermédiaire)

Plan et écarts : `docs/veritable/plans/J5.md`.

### Technologie

- **Données** (`data/veritable/tech/trunk.json`, `tech/branches/{eu,nato,brics,sco}.json`, schéma `data/schemas/tech.ts`) : 61 nœuds au palier 1 et 40 au palier 2 sur dix domaines (énergie, industrie, agriculture, numérique, santé, armement terrestre, marine, air, nucléaire, espace), branches de 7 ou 8 nœuds pour l'UE, l'OTAN, les BRICS et l'OCS ; noms et descriptions en français. Écrites par un agent à partir d'une consigne détaillée (technologies réelles ou annoncées au palier 1, plausibles d'ici 2050 au palier 2, chaînes de prérequis réelles), relues et calibrées ensuite.
- **Premier jour** : indice de développement = 0,6 × PIB par habitant / 60 000 $ + 0,4 × R&D totale / 3,5 % (chacun plafonné à 1 ; R&D totale = recherche publique du budget / 0,35, la part publique de l'ingestion du J2). Une nation a les nœuds dont `adoptedAbove` est sous son indice, ceux qui la nomment dans `adoptedBy` (spécificités : missiles hypersoniques russes, drones turcs et ukrainiens, sous-marins nucléaires d'attaque…), et leurs prérequis. Leurs effets sont dans les données de 2026 : ils ne sont pas appliqués une seconde fois (`baseline`). Part du palier 1 au départ : Royaume-Uni 49 %, France 44 %, Allemagne et Norvège 41 %, Russie 31 %, Italie et Espagne 20 %, Ukraine 16 %, Pologne 13 %, Turquie 11 %.
- **Recherche** : points par mois = R&D totale en points de PIB × 13 (`pointsPerRdPoint`) × modificateurs de recherche × 1,25 si un programme commun d'un bloc de la nation court ; partagés également entre trois projets au plus ; un nœud coûte `cost` × (1 − 0,5 × part des nations simulées qui l'ont) et dure au moins `monthsMin`. L'IA remplit ses projets chaque mois : nœuds disponibles, domaines de son agenda d'abord (les autres pour 0,2), palier 2 pour moitié, le moins cher par unité de préférence, sans tirage. Le joueur commande (`tech-research`, `tech-cancel`).
- **Effets** : capacités de production et de consommation une fois, à l'achèvement ; tant que le nœud est en vigueur, croissance tendancielle, force des divisions (multiplicateur des fronts), puissances aérienne et navale, recherche. Une branche de bloc cesse de compter quand la nation quitte le bloc (c'est la « perte de la branche technologique » de la sortie) ; un projet de branche s'arrête. **Échelle des effets** (`tech.effectScale`) : capacités et croissance 0,3, militaire et recherche 0,5. L'arbre complet donnait, cumulé, alimentation ×1,47, électricité ×1,73, services ×1,59, croissance +1,8 point par an, force terrestre ×3,3 : la croissance tendancielle des données contient déjà le progrès technique du monde, un nœud n'en mesure que l'avance. Sans échelle, le prix mondial de l'alimentation tombait sous la moitié de sa base.
- **Calibrage « 3 % de R&D termine le palier 1 vers 2035 »** : avec les durées minimales écrites par l'agent (6 à 24 mois au palier 1), trois projets en parallèle interdisaient de finir avant 2044 quels que soient les points (658 mois de durées pour les 36 nœuds qui manquent à l'Allemagne). Durées minimales multipliées par 0,5 au palier 1 et 0,45 au palier 2 dans les données. Mesure (10 campagnes sans cœur) : Royaume-Uni (R&D 2,9 %) palier 1 complet en novembre 2035, France et Norvège fin 2038, Allemagne octobre 2038 (elle doit reconstruire toute la filière nucléaire, fermée en 2023), Italie, Espagne, Pologne, Turquie vers 2042, Ukraine au-delà de 2060.
- **Écran Technologie** : points du mois, projets et leur avancement, onglets par domaine et par branche, coût du jour, durée minimale, effets, raison d'un refus (prérequis, file pleine, réservé au bloc).

### Événements

- **Données** (`data/veritable/events/scripted/*.json`, `events/templates/*.json`, schéma `data/schemas/event.ts`) : 50 événements scriptés 2026-2030 et 34 gabarits (33 écrits par un agent, `social-unrest` ajouté au calibrage), textes en français. Ils désignent des fonctions (chef du gouvernement, ministre, état-major, banque centrale), jamais une personne réelle, et n'attribuent aucun crime ni scandale à un dirigeant réel ; les institutions et les lieux réels sont cités (OTAN, détroit d'Ormuz, plan de relance européen). Dont la crise climatique en Asie du Sud-Est (monde), des crises alimentaires et énergétiques, des émeutes contre l'austérité, des incidents qui créent des casus belli entre voisins.
- **Moteur unique** (`sim/events/events.ts`), une fois par mois de jeu (le domaine `events` rejoint l'horloge mensuelle du `Scheduler`, après les blocs) : fenêtre de dates, une fois par nation pour un scripté ou délai de recharge pour un gabarit, conditions sur l'état, probabilité mensuelle tirée au `Rng`. Un événement du monde arrive une fois : ses effets mondiaux (chocs d'offre du reste du monde), puis les choix du joueur. **Effets immédiats** (`effects`, ajout au schéma de DATA-SCHEMAS.md) : ce qui arrive au déclenchement, avant le choix (une émeute est déjà dans la rue).
- **Joueur** : fenêtre surgissante qui met le jeu en pause si l'événement le demande et ouvre l'écran Événements ; au plus deux par mois (les suivants attendent le mois d'après) ; sans réponse au bout d'un mois, le gouvernement tranche comme une IA. **IA** : le choix qui vaut le plus pour son gouvernement (stabilité d'abord, puis argent et croissance selon le poids « croissance » de son agenda, relations selon « influence », armes selon « sécurité » ; un dirigeant agressif aime le grief) ; **un gouvernement se trompe une fois sur cinq** (`aiMistakeProbability`), sans quoi les choix menant aux troubles ou à l'escalade n'étaient jamais pris.
- **Effets** : la stabilité étant recalculée chaque semaine à partir de ses entrées, un effet de stabilité est un choc d'opinion (sur tous les groupes du joueur, sur l'opinion proxy d'une IA) qui se résorbe semaine après semaine. **Troubles** : la stabilité passe 0,07 sous le seuil des troubles (`unrestMargin`), par l'opinion puis, pour ce que l'opinion ne peut porter (l'Ukraine en guerre a déjà une opinion proche de zéro), par la légitimité, qui revient lentement ; première version (choc d'opinion fixe de 0,25) effacée en une semaine par le retour de l'opinion. **Grief** : casus belli « grief » (`war/casus-belli.json`, coût de relations 8) de la nation contre l'autre pendant `months` mois, dans `diplomacy.grievances` ; les griefs expirés sont retirés chaque mois.
- **Journal** : les événements du joueur, du monde, et les scriptés des IA ; les gabarits des IA restent dans l'historique de l'écran (60 derniers), pas dans le journal. Technologie : les nœuds du joueur et les premières mondiales.
- **Écran Événements** : décisions en attente (texte, effets immédiats, chaque choix avec ses effets, date à laquelle le gouvernement tranchera), événements récents de toutes les nations.

### Calibrage de la livraison

- **Troubles et crises** : gabarit `social-unrest` (stabilité sous 0,5, 2 % par mois, recharge 36 mois, troubles immédiats, puis concessions, fermeté ou concertation). Sans lui, aucun trouble en dix ans : les dix événements porteurs de troubles exigent des états rares (déficit au-delà de 4 %, prix de l'alimentation au-delà de 1,15 fois sa base) et mettent les troubles dans le choix que l'IA évite. **Marge des troubles 0,07** (stabilité à 0,28, sous le seuil de révolution de 0,30) : un trouble qui dure dans une nation déjà malheureuse peut finir en révolution.
- **Guerres de l'IA** (règle du J5a conservée : gain espéré sur dix ans contre coût sur trois ans) :
  - **revendication russe ajoutée au scénario** (`europe-10.json`, région `ukraine-oblasts-claimed-by-russia`, contrôleur UKR, revendiquée par RUS) : la Russie revendique depuis septembre 2022 la totalité des quatre oblasts, dont l'Ukraine tient une partie (Zaporijjia, Kherson, Kramatorsk). **Donnée à valider par Lukas.** Sans elle, la Russie n'a aucun casus belli contre l'Ukraine après le cessez-le-feu ;
  - **part des terres prises par point de rapport de force : 6 %** (au lieu de 3 %, plafond 15 % inchangé) : une guerre contre l'Ukraine ne rapportait jamais le dixième de son coût ;
  - **rapport de force minimal 1,5** (au lieu de 2) ;
  - **hostilité requise** : jamais de guerre contre une nation avec qui les relations dépassent −10 (`ai.nations.war.maxRelations`) ; sans cela l'Ukraine mobilisée attaquait la Pologne (un grief, un rapport de force de 4,8, un gain calculé sur le PIB polonais) ;
  - **motif du casus belli** : un casus belli réel double le gain attendu (`casusBelliMotive` = 2).
  - Les guerres intra-européennes restent hors de portée : les sanctions attendues des partenaires de la cible (UE, OTAN) en font le premier coût.
- **Tests A/B isolés des événements** : les tirages des événements suivent l'état, si bien qu'un choc et son témoin se séparent ; les tests headless du choc gazier et de l'embargo, et les rapports du J2 et du J3 (`report.ts`, `reportJ3.ts`), tournent sans événements. Le rapport du J4 et la livraison du J5 les gardent.
- **Performance du runner** : la boucle lisait toute la vue chaque jour de jeu (blocs, votes projetés, coûts des 130 nœuds) : elle suit désormais la date des événements du jour (deux ans sur le cœur : 74 s → 26 s). **Tuiles contestées comptées une fois par mois ou à chaque changement du registre** (`CoreBridge.contestedCounts`) et non à chaque lecture de la vue : 4,8 millions de tuiles parcourues chaque seconde dans le worker du jeu (J5a).

### Livraison du J5 (`docs/veritable/reports/J5/`)

`npx tsx tools/veritable/headless/delivery.ts --runs 100 --years 50 --parallel 5 --seed 1 --core --out <dossier> --report docs/veritable/reports/J5` : 100 campagnes de 50 ans sur le cœur OpenFront et la vraie carte Europe (graines 1 à 100, pilote automatique), réparties sur cinq processus enfants (`child_process`, sans dépendance), 31 minutes, 90 s par campagne en moyenne. `summary.json` (critères, distributions) et `campaigns.csv` (une ligne par campagne) ; les fichiers par campagne restent hors du dépôt.

- **Les treize critères sont tenus.**
- **Nucléaire** : aucun tir dans les cent campagnes (donc aucun en année 1, aucun sous le niveau 2, aucun échange à trois).
- **Guerres nouvelles** : médiane 2 par campagne, maximum 3 ; 134 guerres, aucune sans casus belli ; distribution 0 : 27 campagnes, 1 : 20, 2 : 45, 3 : 8. **133 sont des reprises de la guerre russo-ukrainienne** (territoire contesté), cinq à sept ans après chaque cessez-le-feu ; une seule autre (Ukraine, sur un grief). Les guerres intra-européennes restent hors de portée (sanctions attendues), la Turquie n'a aucun voisin terrestre simulé, les griefs de la Russie contre la Pologne ou la Norvège ne passent pas le calcul coût-gain. Distribution plausible pour la carte Europe, mais étroite : un seul couple de belligérants.
- **Politique** : 6 révolutions (toutes en Ukraine, en 2028-2034, pendant ou juste après une guerre) ; crise politique à dix ans dans 93 campagnes ; junte à dix ans : Russie 7, Turquie 1 ; 65 coups réussis en tout.
- **Économie** : prix mondiaux entre 0,87 et 1,39 fois leur base ; aucun défaut.
- **Technologie** (date médiane du palier 1 complet) : Royaume-Uni décembre 2035, France août 2038, Allemagne et Norvège octobre 2038, Espagne, Pologne, Turquie, Italie 2042-2043, Russie 2044, Ukraine 2070.
- **Événements** : 55 entrées de journal par campagne en moyenne ; **blocs** : 36 décisions par campagne.

### Tests du J3 et du J4 rejoués

- **J3** (`docs/veritable/reports/J3/`, cœur, graine 42, sans bruit ni événements) : embargo UE, prix du gaz à l'import +35 % la première année, PIB russe −4,7 % à deux ans ; France → Espagne, **5 sanctionneurs à six mois** (critère tenu : la question ouverte au J5b devient sans objet), coalition de huit nations, perte de PIB français 426 Md$ contre 192 Md$ de tuiles prises (contestées à 50 %), stabilité 0,479 contre 0,602 pour le témoin ; blocus britannique, commerce maritime norvégien −53 % ; débarquement italien refusé.
- **J4** (`docs/veritable/reports/J4/`, sans cœur, 10 ans × 10 graines 42-51) : les cinq critères tenus (alternance dans les dix nations, crise dans cinq campagnes sur dix, aucun coup dans une démocratie stable, junte russe ou turque dans deux graines au plus, aucune junte sans cause).

### Vérifications

- **`npm test`** : 6 908 tests passent, seuls les trois échecs connus hors jeu restent, aucun rejet non géré ; lint propre.
- **Écrans** vérifiés dans le navigateur (France) : Technologie (27 nœuds sur 61 au départ, 35 points par mois, projet lancé), Événements (fenêtres du 1er mars, du 1er mai, du 1er juin et du 1er août 2026 qui mettent le jeu en pause, choix appliqués et inscrits à l'historique), Blocs (présidence française de l'UE à partir de juillet 2026, votes projetés), section Nucléaire (arsenaux, main morte 45 %, 38 %, 70 %).

### À trancher par Lukas

- **Revendication russe** ajoutée au scénario (voir plus haut).
- **Relations de départ avec la Russie** : elles ne portent pas l'hostilité de 2026 (sanctions de l'UE et du G7 depuis 2022, donnée du J6) ; en 2026, des sanctions de l'UE ou du G7 contre la Russie sont rejetées au vote (Allemagne, Italie, Pologne, Espagne contre). Deux options : attendre le J6 et ses conflits et sanctions initiaux validés à la main ; ou poser dès maintenant, dans le scénario, des relations de départ entre la Russie et les membres de l'UE et de l'OTAN (elles reviendraient ensuite vers l'affinité à +2 par mois).
- **Distribution des guerres** : une seule paire de belligérants en pratique (voir plus haut).
- **Chefs de parti du J5a**, toujours en attente.

## À compléter par Claude Code

- Commit de départ du fork (`upstream-base`) : `4bf92e3c98201326003f790839e04dfcc43ff41a` (« meta: raise saturation midpoints… #5587 »), tag `upstream-base`. Noté le 2026-09-21.
- Échecs de `npm test` connus sur la machine de développement Windows, présents sur le code d'origine et indépendants du jeu — ne pas réparer ; « tests verts » = aucun échec au-delà de ces trois :
  1. `tests/UpdateRegister.test.ts` (2 tests) — exécute le script shell `update.sh`.
  2. `tests/client/clan/ClanDonateDialog.test.ts` (1 test) — format de nombre dépendant de la locale `fr`.
- Chaque modification de `src/core` : fichier, raison, ligne `// VERITABLE:`.
- Chaque schéma zod créé ou modifié : version de `schemaVersion` associée.
